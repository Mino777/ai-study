---
title: "DeepSeek Reasonix 분석: LLM 에이전트 비용 최적화를 위한 Prefix Caching의 재해석"
category: tokenomics
date: "2026-05-25"
tags: [llm, agent, cost-optimization, caching, deepseek, reasonix, tokenomics]
confidence: 1
connections: [tokenomics/claude-code-token-levers-applied-log, tokenomics/claude-code-token-levers-catalog, tokenomics/project-level-cost-analysis-pattern]
status: draft
description: "DeepSeek Reasonix의 Prefix Caching(접두사 캐싱) 원리를 분석하여, 반복적인 ReAct(Reason-Act) 에이전트의 토큰 비용을 획기적으로 절감하는 아키텍처를 학습하고 실제 프로젝트에 적용하는 방안을 탐색합니다."
type: entry
generated_by: gemini
quiz:
  - question: ReAct 기반 LLM 에이전트에서 비용 및 지연 시간 문제가 발생하는 가장 근본적인 원인으로 본문이 지적하는 것은 무엇입니까?
    choices:
      - 에이전트가 사용하는 외부 도구(Tool)의 실행 속도가 느리기 때문이다.
      - 매 추론 단계마다 시스템 프롬프트와 도구 정의 등 변하지 않는 대규모 컨텍스트를 반복적으로 전송하기 때문이다.
      - LLM의 추론(Reason) 성능에 한계가 있어 올바른 결정을 내리기까지 여러 번의 시도가 필요하기 때문이다.
      - 대화 기록이 길어질수록 LLM이 중요한 정보를 잊어버리는 문제가 발생하기 때문이다.
    answer: 1
    explanation: 본문은 ReAct 에이전트의 비효율이 매 단계마다 시스템 프롬프트, 도구 정의, 대화 기록 등 거대한 컨텍스트(Prefix)를 반복적으로 API에 전송하는 것에서 발생한다고 명확히 설명합니다. 이는 전체 토큰의 80-90%가 중복 전송되는 결과를 낳습니다. 다른 선택지들도 LLM 에이전트가 겪을 수 있는 문제들이지만, 본문이 비용 문제의 핵심 원인으로 지목한 것은 '고정된 Prefix의 반복 전송'입니다.
  - question: Reasonix의 'Prefix Caching'이 기존의 단순 프롬프트 캐싱 방식보다 동적인 에이전트 상호작용에 더 효과적인 이유는 무엇입니까?
    choices:
      - 입력 프롬프트의 의미적 유사도를 비교하여 비슷한 질문에 캐시된 답변을 제공하기 때문이다.
      - 클라이언트 측에서 프롬프트를 미리 압축하여 네트워크 전송량을 줄여주기 때문이다.
      - LLM의 내부 KV 캐시를 활용해 프롬프트의 고정된 부분에 대한 계산 결과를 재사용하므로, 뒷부분이 달라져도 캐시를 쓸 수 있기 때문이다.
      - 전체 프롬프트가 과거의 요청과 정확히 일치할 때만 캐시를 사용하여 답변의 정확성을 100% 보장하기 때문이다.
    answer: 2
    explanation: 본문은 Reasonix가 LLM 트랜스포머 아키텍처의 KV 캐시를 직접 활용한다고 설명합니다. 불변하는 Prefix의 KV 계산 결과를 서버에 캐싱해두면, 이후 요청에서는 새로운 부분(Suffix)에 대한 계산만 수행하면 됩니다. 이 덕분에 프롬프트 전체가 일치하지 않아도 캐시 히트가 가능하여 동적인 에이전트 추론에 매우 효과적입니다. 1번은 Semantic Cache, 4번은 일반적인 프롬프트 캐싱에 대한 설명으로, 본문에서 Reasonix와 비교하며 한계를 지적한 방식들입니다.
  - question: 본문의 내용에 비추어 볼 때, 다음 중 Reasonix의 Prefix Caching 전략을 적용했을 때 비용 절감 효과가 가장 클 것으로 예상되는 시나리오는 무엇입니까?
    choices:
      - 사용자가 입력한 문장의 감정을 '긍정', '부정', '중립'으로만 판단하는 일회성 작업.
      - 20개의 전문적인 데이터 분석 도구를 정의하고, 사용자의 복잡한 데이터 분석 요청을 해결하기 위해 5~6단계의 추론과 도구 사용을 반복하는 에이전트.
      - "매 대화마다 에이전트의 역할(예: 시인, 코딩 교사)과 사용 가능한 도구가 완전히 재설정되는 동적인 환경."
      - 자주 묻는 질문(FAQ)에 대해 거의 동일한 답변을 제공하는 고객 서비스 챗봇.
    answer: 1
    explanation: 본문은 Prefix Caching이 '긴 시스템 프롬프트와 다수의 도구 정의'를 가지고 '여러 번의 도구 호출이 필요한 작업'에서 진가를 발휘한다고 강조합니다. 2번 시나리오는 Prefix의 크기가 매우 크고(20개의 도구 정의), 추론의 '턴'이 길어(5~6단계) 중복 전송을 막는 효과가 누적되므로 비용 절감 효과가 가장 큽니다. 1번은 일회성 작업이라 캐싱의 이점이 없고, 3번은 Prefix가 계속 변경되어 캐싱 오버헤드가 발생할 수 있으며, 4번은 Reasonix보다 Semantic Cache가 더 적합한 사례입니다.

---

LLM 에이전트를 프로덕션 환경에 배포하려는 시도가 늘어나면서, 아이디어의 실현 가능성만큼이나 '비용'이라는 현실적인 장벽이 중요해졌습니다. 특히 여러 도구를 사용하고 복잡한 추론을 수행하는 ReAct(Reason-Act) 기반 에이전트는 매 단계마다 시스템 프롬프트, 도구 정의, 대화 기록 등 거대한 컨텍스트를 반복적으로 API에 전송합니다. 이는 전체 토큰의 80-90%가 동일한 내용으로 중복 전송되는 비효율을 낳으며, 그대로 비용과 지연 시간(latency) 증가로 이어집니다. 이 문제를 해결하지 못하면 정교한 에이전트는 '데모' 수준을 넘어서기 어렵습니다.

DeepSeek-AI가 발표한 Reasonix는 바로 이 문제를 정면으로 겨냥합니다. 단순한 프롬프트 캐싱을 넘어, LLM의 내부 동작 방식인 KV 캐시를 활용하는 'Prefix Caching'을 통해 에이전트의 추론 과정에서 발생하는 중복 토큰 비용을 극적으로 줄이는 방법을 제안합니다. 이 글에서는 Reasonix의 접근법을 분석하고, 시니어 iOS 개발자가 이 개념을 이해하여 자신의 AI 프로젝트에 어떻게 적용할 수 있을지 구체적인 방향을 제시합니다.

## ReAct 에이전트의 비용 구조와 근본적 한계

표준적인 ReAct 에이전트의 작동 방식은 토큰 비효율성을 내재하고 있습니다. 사용자의 요청을 처리하기 위해 에이전트는 '생각(Reason)'하고 '행동(Act)'하는 과정을 반복합니다.

1.  **Reason**: 현재 상황과 목표를 고려하여 다음에 어떤 도구를 사용할지 결정한다.
2.  **Act**: 결정된 도구를 실행하고 그 결과를 관찰(Observation)한다.
3.  **Repeat**: 관찰 결과를 바탕으로 최종 답을 찾거나 다음 행동을 결정하기 위해 1번으로 돌아간다.

문제는 매 반복마다 LLM에 전달되는 컨텍스트의 구조에 있습니다.

`[System Prompt] + [Tool Definitions] + [Chat History] + [Previous (Reason, Act, Observation)] + ...`

여기서 `[System Prompt]`, `[Tool Definitions]`, `[Chat History]` 등은 추론 과정 내내 거의 변하지 않는 'Prefix(접두사)'입니다. 에이전트가 도구를 한 번 호출하고 그 결과를 받아 다음 추론을 이어갈 때, 이 거대한 Prefix가 또다시 전송됩니다. 이는 마치 Swift 앱에서 `viewDidLoad`에 있어야 할 초기 설정 코드를 `viewDidLayoutSubviews`에서 매번 반복 실행하는 것과 같은 비효율입니다.

### Prefix Caching: 단순 캐싱을 넘어서

기존의 프롬프트 캐싱은 입력 프롬프트 전체가 정확히 일치할 때만 작동하여, 동적인 에이전트 상호작용에서는 캐시 히트율이 극히 낮습니다. 하지만 Reasonix의 Prefix Caching은 LLM의 트랜스포머 아키텍처 자체를 활용합니다.

LLM은 토큰을 처리할 때 각 토큰에 대한 Key-Value(KV) 쌍을 계산하여 어텐션(attention) 메커니즘에 사용합니다. 이 KV 쌍은 한 번 계산되면 다음 토큰을 처리할 때 재사용될 수 있습니다. Reasonix는 이 점에 착안하여, 불변하는 Prefix에 대한 KV 계산 결과를 서버의 GPU 메모리에 캐싱합니다.

다음 호출 시에는 전체 Prefix를 다시 보내는 대신, 캐시된 Prefix를 가리키는 ID(예: `prefix_id`)와 새로운 추가 정보(예: 도구 호출 결과)만 전송합니다. 서버는 해당 ID를 이용해 저장된 KV 캐시를 즉시 로드하고, 새로운 토큰에 대한 계산만 수행합니다. 이로써 입력 토큰 길이를 획기적으로 줄여 비용과 첫 토큰 생성 시간(TTFT, Time to First Token)을 모두 단축합니다.

```mermaid
graph TD
    subgraph "Standard ReAct Agent"
        A[Initial Prompt] -- "Full Context (Prefix + Query)" --> B((LLM API))
        B -- "Tool Call" --> C{Execute Tool}
        C -- "Tool Result" --> D[Append Result]
        D -- "Full Context AGAIN (Prefix + Query + Result)" --> E((LLM API))
        E -- "Final Answer" --> F[Output]
    end

    subgraph "Reasonix with Prefix Caching"
        subgraph "Turn 1"
            SA["Static Prefix:\n(System Prompt, Tool Defs...)"] -- "Prefix sent once" --> SB["("(LLM Engine: Compute & Cache KV""])
            SB -- "Cache ID: 'prefix_123' generated" --> SC[Client Stores ID]
            SQ["Dynamic Suffix:\n(Initial Query)"] -- "Suffix sent" --> SB
            SB -- "Tool Call" --> SD{Execute Tool}
        end
        subgraph "Turn 2"
            SE["Tool Result (New Suffix)"] -- "+ 'prefix_123'" --> SF["("(LLM Engine: Load KV Cache""])
            SF -- "Processes only new tokens" --> SG[Final Answer]
        end
    end
```

이 다이어그램은 두 접근 방식의 차이를 명확히 보여줍니다. 표준 에이전트는 매번 전체 컨텍스트를 전송하지만, Reasonix는 첫 호출에서 Prefix를 캐싱하고 이후에는 새로운 정보와 캐시 ID만으로 통신합니다.

## 캐싱 전략 비교: 언제 Reasonix가 빛을 발하는가

모든 상황에 Prefix Caching이 최적인 것은 아닙니다. 프로젝트의 특성에 따라 적합한 캐싱 전략은 달라질 수 있습니다.

| 전략 | 주요 원리 | 장점 | 단점 | 최적 사용 사례 |
| --- | --- | --- | --- | --- |
| **No Caching** | 모든 요청을 독립적으로 처리 | 구현이 가장 단순함. 상태 관리가 필요 없음. | 비용과 지연 시간이 가장 높음. | 일회성, 비대화형 작업. |
| **Semantic Cache**<br />(e.g., GPTCache) | 입력 프롬프트의 의미적 유사도를 기반으로 이전 응답 반환 | 동일하거나 유사한 질문에 대한 반복 비용 절감. | 캐시 히트율이 낮음. 에이전트의 동적 추론 과정에는 부적합. | FAQ 챗봇, 일반적인 질문 응답. |
| **Reasonix Prefix Cache** | LLM의 KV 캐시를 활용하여 프롬프트의 불변 접두사를 캐싱 | 다단계 추론/도구 사용 에이전트의 비용 및 지연 시간 대폭 감소. | 서버 측 지원이 필수적임. 캐시 관리의 복잡성 증가. | 복잡한 도구 사용 에이전트, 코드 생성/디버깅, 데이터 분석. |

Reasonix의 접근법은 특히 다음과 같은 상황에서 진가를 발휘합니다.
- **긴 시스템 프롬프트와 다수의 도구 정의**: Prefix의 크기가 클수록 절감 효과가 커집니다.
- **여러 번의 도구 호출이 필요한 작업**: 추론의 '턴(turn)'이 길어질수록 중복 전송을 막는 효과가 누적됩니다.
- **정해진 역할 내에서 작동하는 에이전트**: 시스템 프롬프트나 역할이 자주 바뀌지 않는 경우 캐시를 재사용하기 용이합니다.

반면, 매 대화마다 에이전트의 역할이나 사용 가능한 도구가 완전히 바뀌는 동적인 환경에서는 Prefix가 계속 변경되므로 캐싱의 이점이 줄어들 수 있습니다. 이는 오히려 캐시를 생성하고 폐기하는 오버헤드를 유발할 수 있습니다.

### Swift 개발자의 관점: 프로젝트 적용 방안

iOS 개발자가 이 개념을 `ai-study/tarosaju` 같은 프로젝트에 적용한다고 가정해 봅시다. `tarosaju`는 사용자의 사주 정보를 기반으로 다양한 해석(도구)을 제공하는 에이전트가 될 수 있습니다.

1.  **사주 정보 분석 요청**: 사용자가 자신의 생년월일시를 입력합니다.
2.  **Prefix 생성 및 캐싱**:
    -   **Prefix**: `[시스템 프롬프트: "당신은 명리학 전문가입니다..."] + [도구 정의: "만세력 조회", "오행 분석", "대운 분석"...] + [사용자 사주 정보]`
    -   이 Prefix를 Reasonix를 지원하는 백엔드 API에 보내 KV 캐시를 생성하고 `saju_cache_id`를 받습니다.
3.  **다단계 추론**:
    -   **1단계**: `saju_cache_id`와 함께 `"먼저 만세력을 조회해줘"`라는 요청을 보냅니다. API는 캐시를 로드하고 '만세력 조회' 도구를 호출하라는 응답을 줍니다.
    -   **2단계**: '만세력 조회' 결과를 받은 후, `saju_cache_id`와 함께 `[만세력 결과] + "이 결과를 바탕으로 오행을 분석해줘"`를 전송합니다.
    -   이 과정에서 거대한 시스템 프롬프트와 도구 정의는 단 한 번만 전송됩니다.

Swift 클라이언트 코드에서는 이 `cache_id`를 상태로 관리하는 로직이 필요합니다.

```swift
// 개념 증명을 위한 Swift 의사 코드
// 실제로는 백엔드 API와의 통신을 통해 처리됩니다.

class SajuAgent {
    private var conversationId: String
    private var prefixCacheId: String?

    // 에이전트와 대화 시작
    func startConversation(with sajuInfo: SajuInfo) async throws -> String {
        let systemPrompt = "당신은 명리학 전문가입니다..."
        let toolDefinitions = "[Tool: getMangseryuk, analyzeOhaeng, ...]"
        let prefix = "\(systemPrompt)\n\(toolDefinitions)\n\(sajuInfo.description)"

        // 1. Prefix를 보내고 cacheId를 받는다.
        let initialResponse = await apiClient.createPrefixCache(prefix: prefix)
        self.prefixCacheId = initialResponse.cacheId

        // 2. 첫 번째 질문을 cacheId와 함께 보낸다.
        let firstQuery = "이 사주를 전체적으로 분석해주세요."
        return await continueConversation(with: firstQuery)
    }

    // 대화 이어가기
    func continueConversation(with message: String) async throws -> String {
        guard let cacheId = prefixCacheId else {
            throw AgentError.notInitialized
        }

        // Prefix 전체 대신 cacheId와 새로운 메시지만 보낸다.
        let response = await apiClient.sendMessage(cacheId: cacheId, suffix: message)

        // 응답이 도구 호출이라면 도구를 실행하고 결과를 다시 보낸다.
        if let toolCall = response.toolCall {
            let toolResult = executeTool(toolCall)
            return await continueConversation(with: toolResult.description)
        } else {
            // 최종 답변 반환
            return response.finalAnswer
        }
    }

    private func executeTool(_ toolCall: ToolCall) -> ToolResult {
        // ... 도구 실행 로직 ...
        return ToolResult()
    }
}
```

이 Swift 코드는 클라이언트가 어떻게 `prefixCacheId`를 관리하며 백엔드와 상호작용할 수 있는지 보여주는 개념적 예시입니다. 실제 KV 캐싱 로직은 전적으로 DeepSeek Reasonix와 같은 기술을 지원하는 서버 인프라에서 처리됩니다. 따라서 현실적인 적용은 이러한 기능을 제공하는 모델 제공자(MaaS)를 선택하거나 자체 백엔드에 유사한 로직을 구현하는 것을 전제로 합니다.

## 자기 점검

### 이해도 질문

1.  기존의 의미 기반 캐시(Semantic Cache)와 Reasonix의 Prefix Caching은 에이전트의 다단계 추론 상황에서 왜 다른 효율을 보이나요?
2.  Prefix Caching 전략의 효과가 감소하거나 오히려 비효율적일 수 있는 시나리오는 무엇일까요?
3.  LLM의 'KV 캐시'가 무엇이며, 이것이 어떻게 Prefix Caching을 기술적으로 가능하게 하는지 설명해 보세요.

### 프로젝트 적용 질문

1.  현재 진행 중인 프로젝트(또는 구상 중인 아이디어)에서 반복적으로 호출되지만 변하지 않는 컨텍스트(Prefix)는 무엇인가요? 이 Prefix를 분리하여 캐싱한다면 비용과 성능에 어느 정도의 영향을 미칠 것으로 예상되나요?