---
title: Harness Journal 021 — GITHUB_TOKEN 정책으로 침묵하는 deploy 체인 (ai-review.yml 맹점)
category: harness-engineering
series: harness-journal
date: '2026-04-13'
tags:
  - harness-journal
  - ci-cd
  - github-actions
  - ai-review
  - deploy
  - workflow-chaining
  - github-token
  - silent-failure
confidence: 4
connections:
  - harness-engineering/harness-journal-003-squash-merge-trap-pattern
  - harness-engineering/harness-journal-014-hub-auto-merge-inbound-tips
  - harness-engineering/harness-journal-020-promise-timeout-wrapper
  - harness-engineering/compound-engineering-philosophy
  - harness-engineering/harness-journal-022-qa-gate-exit-code-trap
status: complete
description: >-
  ai-review.yml 기반 auto-merge 파이프라인을 쓰는 모든 프로젝트는 squash merge가 downstream on:
  push 워크플로우(예: deploy.yml)를 조용히 건너뛴다. 원인은 GitHub의 GITHUB_TOKEN 정책(무한 루프 방지). 4번
  연속 머지 후 프로덕션 stale 상태로 유저가 500 마주친 후에야 드러남. 3 수정 옵션 + 즉시 복구 스니펫 + 일반화.
type: entry
quiz:
  - question: >-
      `ai-review.yml`이 `GITHUB_TOKEN`으로 squash merge한 뒤 `deploy.yml`(on: push:
      main)이 자동 실행되지 않는 이유는?
    choices:
      - 권한 부족
      - Rate limit
      - >-
        GitHub의 명시적 정책 — `GITHUB_TOKEN`으로 발생한 `push` 이벤트는 **다운스트림 워크플로우를 깨우지
        않는다** (무한 루프 방지용)
      - 네트워크 지연
    answer: 2
    explanation: >-
      GitHub Actions 공식 문서: *"events triggered by the GITHUB_TOKEN, with the
      exception of workflow_dispatch and repository_dispatch, will not create a
      new workflow run."* 재귀 워크플로우 루프 방지를 위해 의도적으로 설계된 정책. 예외는
      `workflow_dispatch`와 `repository_dispatch` 두 개뿐. 구조적 제약이라 권한/토큰 문제가 아니고 해결
      불가능한 bug도 아님 — *우회* 필요.
  - question: 이 사이런트 체인 실패가 *왜 일찍 감지되지 않았나*의 핵심은?
    choices:
      - 테스트가 부족해서
      - >-
        **merge 성공 ≠ deploy 성공**이지만 UI는 둘을 구분하지 않음. GitHub Merged 배지 / git log
        최신 커밋 / CI 초록불 모두 *배포됐다*는 잘못된 시그널을 준다. 사용자가 페이지를 여는 순간 500이 뜰 때까지 드러나지
        않는다
      - 로그가 부족해서
      - Vercel 측 버그
    answer: 1
    explanation: >-
      관찰 가능성의 세 층(머지 상태 / 배포 상태 / 프로덕션 상태)이 UI에 섞여 있으면 실제로는 다른 것임에도 *같은 것*으로
      인식된다. 이게 바로 사일런트 실패의 본질. 처방: 회고/compound 체크리스트에 *머지 성공 + deploy 실행 확인 +
      프로덕션 200* 3단계를 명시적으로 분리해야 함.
  - question: >-
      3 수정 옵션(workflow_dispatch / workflow_run / PAT) 중 *권장되는 옵션 1
      (workflow_dispatch)*의 가장 큰 장점은?
    choices:
      - 실행 속도
      - 코드량이 가장 적음
      - >-
        **의도가 코드에 남음** — "여기서 deploy를 일부러 깨운다"는 명시적 step이 있어 다음 사람이 읽을 때 체인을
        추적하기 쉬움. 기존 GITHUB_TOKEN 권한으로 실행 가능 (PAT 불필요)
      - Vercel 호환성
    answer: 2
    explanation: >-
      옵션 2(workflow_run)는 deploy.yml 쪽에 트리거를 숨겨서 "왜 이 deploy가 실행됐지?" 추적이 어렵다. 옵션
      3(PAT)은 *새로운 사일런트 실패 모드*를 만든다 — PAT 만료 시 전체 체인이 조용히 깨짐. 옵션 1은 코드에 *의도*가
      선명하게 남아 Compound Engineering의 "행동에 박는 가드 > 기억에 의존하는 가드" 원칙과 일치.
---

## 한 줄 요약

`ai-review.yml`이 `GITHUB_TOKEN`으로 squash merge하면 `deploy.yml`(on: push: main)이 **침묵** — GitHub의 무한 루프 방지 정책이 원인이다. moneyflow에서 4번 연속 머지 후 프로덕션은 `3682016`에 stale 상태로 남았고, `<Analytics beforeSend={fn}>` RSC 직렬화 버그가 그 stale 버전에 섞여 있는 채 유저가 페이지 열고 500 마주칠 때까지 드러나지 않음. **구조적 CI 결함 + 명시적 GitHub 정책 + 사일런트 실패 3박자**.

---

## 배경 — 2026-04-13 moneyflow 사건

### 타임라인

| Time (UTC) | SHA | 트리거 경로 | `deploy.yml` 실행? |
|---|---|---|---|
| 04:35 | `cf52ead` | 직접 main push | ✅ |
| 04:37 | `c2ef57a` | 직접 main push | ✅ |
| 04:48 | `3682016` | 직접 main push | ✅ (마지막 정상 배포) |
| 05:43 | `5aeefa6` (#114) | ai-review squash merge | ❌ 침묵 |
| 06:13 | `b521b4e` (#115) | ai-review squash merge | ❌ 침묵 |
| 06:26 | `309e6e4` (#116) | ai-review squash merge | ❌ 침묵 |
| 07:41 | `a981714` (#117) | ai-review squash merge | ❌ 침묵 |
| 07:28 | — | **유저가 페이지 열고 500 인지** | — |
| 07:43 | `a981714` | **수동 `gh workflow run`** | ✅ (긴급 복구) |

→ ai-review auto-merge 도입 이후 **100% deploy 체인 실패율**. 4번 연속 머지됐지만 프로덕션은 `3682016`에 고정됐고, v0.9.37(`f6901a2`)에서 추가된 `<Analytics beforeSend={fn}>` RSC 직렬화 버그가 그 stale 버전에 포함된 채 유저가 페이지 열 때까지 숨어있었다.

### 왜 일찍 드러나지 않았나

- **merge 성공 ≠ deploy 성공이지만 UI는 구분을 안 한다**: GitHub "Merged" 배지, `git log origin/main` 최신 커밋, CI 상태 초록불 — 모두 "배포됐다"는 잘못된 시그널을 준다.
- **유저가 API endpoint만 쓰면 증상이 안 보인다**: `beforeSend` 버그는 페이지 SSR에서만 터지고 `/api/*`는 정상. 개발 중 API 테스트만 하다가 페이지를 처음 여는 순간 500.
- **Vercel Git 통합 있다고 *착각*하기 쉽다**: 많은 Vercel 프로젝트가 git 통합으로 자동 배포한다. `deploy.yml`이 있는 프로젝트는 "어차피 Vercel이 알아서 배포하겠지"라고 넘길 수 있지만, 이 프로젝트는 git 통합을 쓰지 않고 `deploy.yml`이 유일한 배포 경로였다.

---

## 근본 원인 — GitHub의 명시적 정책

[GitHub Actions 공식 문서](https://docs.github.com/en/actions/writing-workflows/choosing-when-your-workflow-runs/triggering-a-workflow#triggering-a-workflow-from-a-workflow):

> *"When you use the repository's `GITHUB_TOKEN` to perform tasks, events triggered by the `GITHUB_TOKEN`, with the exception of `workflow_dispatch` and `repository_dispatch`, will not create a new workflow run."*

`ai-review.yml`은 PR을 squash merge한 뒤 `git push origin HEAD:main`을 `GITHUB_TOKEN`으로 실행한다. 이 push는 git 관점에서는 정상 push지만, GitHub Actions 관점에서는 *의도적으로* downstream `on: push: main` workflow를 깨우지 않는다. 의도는 **무한 워크플로우 루프 방지** — workflow A push → workflow B → workflow B push → workflow A → ... 의 재귀를 막기 위해 `GITHUB_TOKEN`으로 만들어진 push는 `push` 이벤트를 *발행하지 않는다*.

이 정책은 일반에 알려진 사실이지만, 실제로 마주하기 전까지 인지되지 않는다. `ai-review.yml` 자체는 "머지 성공"을 반환하고, downstream 워크플로우가 "안 돌아서" 드러나는 에러는 *없다*. 사일런트 실패는 모니터링하기 어렵다.

---

## 해결 — 3 수정 옵션 (침습도 낮은 순)

### 옵션 1 — `ai-review.yml`에 `workflow_dispatch` 호출 추가 (권장)

```yaml
# .github/workflows/ai-review.yml
# 기존 merge step 이후에 추가
- name: Trigger deploy workflow (bypass GITHUB_TOKEN push event silence)
  if: steps.merge.outputs.merged == 'true'
  uses: actions/github-script@v7
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    script: |
      await github.rest.actions.createWorkflowDispatch({
        owner: context.repo.owner,
        repo: context.repo.repo,
        workflow_id: 'deploy.yml',
        ref: 'main',
      })
```

**장점**: 가장 명시적. "여기서 deploy를 일부러 깨운다"는 의도가 코드에 남아 다음 사람이 읽을 때 체인을 추적하기 쉽다. 기존 `GITHUB_TOKEN` 권한(`actions: write`)으로 `workflow_dispatch` 호출 가능 — PAT 불필요.

**단점**: `workflow_dispatch` 호출 자체가 실패하면 그것도 무음 drop. 실패 감지 필요 시 step conclusion 체크로 방어.

### 옵션 2 — `deploy.yml` 트리거를 `workflow_run`으로 전환

```yaml
# .github/workflows/deploy.yml 상단
on:
  workflow_run:
    workflows: ["🚀 Auto Merge to main"]   # ai-review.yml의 name 필드와 정확히 일치해야 함
    types: [completed]
    branches: [main]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    # ...
```

**장점**: `ai-review.yml`을 건드리지 않고 `deploy.yml`만 수정하면 됨. 이식 시 `ai-review.yml`의 유사도를 깨지 않음.

**단점**: `workflow_run`은 *독립 실행*이라 `ai-review.yml` 컨텍스트(PR 번호 등)를 기본으로 받지 못함. 필요하면 `github.event.workflow_run.head_sha`로 lookup해야 함. `conclusion == 'success'` 체크 필수 (실패한 run에도 트리거되므로).

### 옵션 3 — PAT(`PAT_FOR_CI_CHAIN`) 사용

```yaml
# .github/workflows/ai-review.yml
token: ${{ secrets.PAT_FOR_CI_CHAIN }}
```

**장점**: GITHUB_TOKEN 제약을 완전히 우회. "GITHUB_TOKEN 정책"을 다시 생각할 필요 없음.

**단점**: PAT 관리(로테이션, 권한 최소화, 소유자 종속) 오버헤드. **PAT 만료 시 *조용히* 전체 체인이 깨지는 새 실패 모드**가 생긴다. 리스크가 낮지 않다.

---

## 즉시 복구 — 수동 트리거 스니펫

근본 해결 적용 전까지는 매 머지 후 수동 트리거로 대응:

```bash
# 1. 최신 main SHA로 deploy.yml 수동 트리거
gh workflow run "🚀 Deploy to Vercel" --ref main

# 2. 트리거 확인 (2~3초 대기 후)
gh run list --workflow "🚀 Deploy to Vercel" --limit 1 \
  --json headSha,status,conclusion

# 3. 프로덕션 스모크 테스트 (배포 완료 후)
for p in / /login /signup /api/health; do
  curl -s -o /dev/null -w "$p = %{http_code}\n" "https://<host>$p"
done
```

이 스니펫은 회고/compound 사이클의 *필수 체크리스트*로 편입해야 한다. **"머지 성공 = 끝"이 아니라 "머지 성공 + deploy 실행 확인 + 프로덕션 200 = 끝"**.

---

## 일반화 — 이 제약이 영향을 주는 다른 downstream 체인

`deploy.yml`뿐 아니라 *`on: push: main`으로 트리거되는 모든 downstream 워크플로우*에 동일하게 적용:

- Post-merge 태깅/릴리스 (`release.yml`)
- Post-merge 문서 사이트 재빌드 (`docs.yml`)
- Post-merge canary 모니터링 (`canary.yml`)
- Post-merge Slack/Telegram 알림 워크플로우
- Post-merge DB 마이그레이션 (`db-migrate.yml`)

체크 방법:

```bash
# 전 워크플로우 최근 실행 + 트리거 경로 확인
gh run list --event push --branch main --limit 20 \
  --json name,headSha,event,createdAt

# ai-review 머지 직후 downstream 워크플로우가 같은 SHA에 대해 1:1로 매칭되는지 관찰
# 매칭 안 되면 그 워크플로우도 침묵 실패 중
```

---

## 이식 대상 — 3 프로젝트 공통

| 프로젝트 | `deploy.yml` / downstream | 영향도 | 권장 옵션 |
|---|---|---|---|
| **moneyflow** | `deploy.yml` (Vercel, 유일 배포 경로) | 🔴 이미 P0 장애 경험 | 옵션 1 즉시 적용 |
| **tarosaju** | Vercel Git 통합 (git 이벤트 기반) | 🟡 영향 없음 (git 이벤트는 GITHUB_TOKEN 제약 밖) | 옵션 1 선제 적용 권장 |
| **ai-study** | Vercel Git 통합 | 🟢 영향 없음 | 옵션 1 선제 적용 검토 |

**이식 사이클**: 옵션 1을 템플릿화해서 3 프로젝트에 순차 이식 + 각 프로젝트에서 1회 dogfooding. 박제 전이성 사이클로 편입.

---

## 패턴 일반화 — "observed ≠ happened" 계열 사일런트 실패

이 pattern은 Journal 003 (squash merge trap)의 *CI 확장*판이다. 다음과 같은 계열 함정들:

| 함정 | "관찰된 것" | "실제 발생한 것" |
|---|---|---|
| **이 엔트리** | PR Merged 배지 | 배포는 안 됨 |
| [Journal 003](/wiki/harness-engineering/harness-journal-003-squash-merge-trap-pattern) | 로컬 git이 origin과 일치 | 로컬 브랜치가 origin/main에서 divergent (`/wt-branch` 탄생 배경) |
| `setTimeout` retry cleanup 누락 | 컴포넌트 unmount | 타이머는 여전히 실행됨 (좀비) |
| `Promise.allSettled` timeout 없음 | 모든 promise가 settle 대기 | 하나가 무한 pending이면 전체 무한 pending |
| `supabase.channel(name).unsubscribe()` | 구독 해제됨 | *새* 인스턴스가 unsubscribe됨, 원본은 그대로 |

**공통 처방**: UI/API/문서가 주는 시그널을 그대로 믿지 말고, **실측 단계**를 1개 이상 더 추가해야 한다. 머지됐다고 배포된 게 아님. 타이머 변수가 사라졌다고 실행이 멈춘 게 아님. Unsubscribe 호출했다고 구독이 해제된 게 아님.

Compound Engineering의 *"행동에 박는 가드 > 기억에 의존하는 가드"* 원칙의 배경: 기억에 의존하면 "당연히 그렇겠지" 가정이 silent failure를 낳는다. 가드는 그 가정을 *행동으로 검증*한다.

---

## `/curate-inbound` dogfooding 메타 (워커 push 방식 첫 실전)

이 Journal은 **`/curate-inbound` 슬래시 커맨드의 워커 push 방식 첫 실전 dogfooding** 결과물이다. (Journal 020은 허브 pull 방식이었음.)

### 검증 결과 (실제로 일어난 일)

| 단계 | 결과 |
|---|---|
| 워커(moneyflow 세션)가 `patterns/2026-04-13-github-token-deploy-chain-fix` push | ✅ |
| `ai-review.yml` 자동 PR 생성 (#38) | ✅ |
| Test Gate (`npm test + npm run build`) | ✅ 통과 |
| `patterns` + `needs-curation` 라벨 부착 | ✅ |
| Auto-merge SKIP | ✅ (이번 세션의 ai-review.yml 버그 fix가 실효화된 순간) |
| 허브 세션에서 `/curate-inbound #38` 실행 | ✅ |
| 분류: **journal** 판정 (215줄 + incident + 구조적 결함) | ✅ |
| 파일 rename: `ai-review-yml-downstream-workflow-chain-pattern.mdx` → `harness-journal-021-*.mdx` | ✅ |
| Frontmatter 정리 (series 추가, status draft→complete, 비표준 필드 제거, quiz 3문항 추가) | ✅ |
| `curate/harness-journal-021-*` 브랜치 push | ✅ |

### 발견 사항

- **워커가 이미 wiki 포맷**으로 작성 → curation 작업량의 70%가 frontmatter 정리 + 분류 결정
- **워커가 "승격 노트" 섹션으로 분류 추천을 줌** — 허브의 분류 결정을 돕는 좋은 관행. 다음 `/curate-inbound` 버전에 이 convention 박제 가능
- **Relative links(`./foo.mdx`)** 패턴이 초기 draft에서 자주 발견됨. curation 단계에서 절대경로 변환 필수
- **비표준 frontmatter 필드**(`source`, `incident`)는 zod schema가 silent strip. 로그로 경고 띄우는 게 안전 (다음 validate-content.mjs 개선 후보)

---

## 다음 큐

- 🔴 **옵션 1을 3 프로젝트에 이식** — moneyflow 최우선 (P0 장애 재발 방지), tarosaju/ai-study 선제 적용
- 🟡 **`.github/workflows/ai-review.yml` 템플릿화** — 3 프로젝트 공통 관리 (개별 tweak은 최소화)
- 🟡 **회고/compound 체크리스트에 "머지 + deploy + 프로덕션 200 3단" 명시** — 사일런트 실패 방어
- 🟢 **다른 downstream 체인 grep 감사** — `release.yml` / `docs.yml` / `canary.yml` / `db-migrate.yml`이 있는지, `on: push: main`인지

---

## 핵심 메시지

> **"merge 성공 = 배포 성공"이라는 직관이 오늘 4번 틀렸고, 4번 모두 사일런트였다.** GitHub의 `GITHUB_TOKEN` 정책은 무한 루프 방지라는 좋은 의도지만, 그 결과로 `ai-review.yml`을 쓰는 모든 프로젝트가 downstream 워크플로우를 *의도적으로 일깨워야 하는* 책임을 갖게 된다. 이 책임이 *코드에 명시적으로 박혀 있지 않으면* 매 머지마다 기억에 의존하게 되고, 기억은 반드시 한 번은 실패한다.

Compound Engineering이 말하는 *"행동에 박는 가드 > 기억에 의존하는 가드"* 원칙이 CI 레이어에도 그대로 적용된다: 옵션 1의 `workflow_dispatch` step은 그 가드를 **코드에 박는다**. 옵션 3의 PAT는 기억(PAT 갱신)에 의존한다 — 그래서 권장되지 않는다.

## 관련

- [Harness Journal 003 — squash merge trap pattern](/wiki/harness-engineering/harness-journal-003-squash-merge-trap-pattern) — 로컬 git에서 관찰된 같은 계열 함정. 이 엔트리는 그 패턴의 *워크플로우 그래프 확장*판
- [Harness Journal 014 — hub auto-merge inbound tips](/wiki/harness-engineering/harness-journal-014-hub-auto-merge-inbound-tips) — `ai-review.yml` 허브 이식 최초 사이클. 이 엔트리는 그 이식 결과의 *맹점*을 폭로
- [Harness Journal 020 — promise timeout wrapper](/wiki/harness-engineering/harness-journal-020-promise-timeout-wrapper) — 같은 날 moneyflow에서 박제된 다른 사일런트 실패 계열
- [Compound Engineering Philosophy](/wiki/harness-engineering/compound-engineering-philosophy) — "행동에 박는 가드 > 기억에 의존하는 가드" 원칙의 출처
