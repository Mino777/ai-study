# iOS AI 면접 플래시카드 (암기용)
## 한국 빅테크 2026년 상반기 면접 준비

**사용법**: 각 카드를 따로 출력해서 한글 면(앞)만 보고 답변 연습하기. 영어 정의는 참고용.

---

## 카테고리 1️⃣: AI 코딩 도구 (5장)

### Card 1.1
**[한글 앞면]**
```
Q. Claude Code와 Cursor의 핵심 차이점은?

답변 시간 목표: 30초
```

**[영어 뒷면 - 참고]**
```
Claude Code: Deep reasoning, 1M tokens, 5.5x token efficiency
Cursor: Interactive editing, IDE integration, fast JSX

선택 기준:
- 깊이 있는 리팩토링 → Claude Code
- 빠른 스타일 조정 → Cursor
```

---

### Card 1.2
**[한글 앞면]**
```
Q. iOS 개발에서 Claude Code의 1백만 토큰 컨텍스트를 
   어떻게 배분하는가?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
배분 예시 (1M tokens):
- System prompt & guides: 20K (CLAUDE.md, 스타일)
- Project structure & types: 200K (Model, ViewModel)
- Existing code samples: 500K (완성된 코드 예시)
- Current work: 280K (메시지 왕복)

⚠️ 60% 초과 시 새 세션 또는 컨텍스트 압축
```

---

### Card 1.3
**[한글 앞면]**
```
Q. AI 생성 iOS 코드 리뷰에 주간 11.4시간을 줄이는 
   3가지 효율화 방법은?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
효율화 방법:
1. 패턴 라이브러리 (예시 코드 제시)
   → AI가 일관성 있게 생성 (수동 리뷰 시간 50% 감소)

2. SwiftLint + 자동 검사
   → 문법/구조 자동 검증 (200줄 코드 5분 → 30초)

3. 체크리스트 기반 리뷰
   → 일관성, 메모리 안전, 동시성, 에러 처리 (기계적 검사)
```

---

### Card 1.4
**[한글 앞면]**
```
Q. Claude Code를 사용할 때, 어떤 상황에는 
   사용하지 말아야 하는가?

답변 시간 목표: 45초
```

**[영어 뒷면 - 참고]**
```
사용 금지 상황:
✗ 보안 로직 (인증, 암호화)
✗ 성능 크리티컬 알고리즘 (정렬, 검색)
✗ 개인정보 처리 (Keychain, 민감 데이터)
✗ 법적/규정 요구사항 (GDPR, 한국 개인정보보호법)

결론: "AI는 속도를 올리는 도구지, 책임을 피하는 도구 아님"
```

---

### Card 1.5
**[한글 앞면]**
```
Q. 회사가 "당신은 AI 도구를 얼마나 신뢰하는가?"라고 물으면?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
모범 답변:
"AI는 협력자이지 대체자 아님.

신뢰 수준:
- 보일러플레이트: 70% (테스트는 수동)
- UI 레이아웃: 85% (비주얼은 확인)
- 보안/암호화: 0% (100% 수동)

증거: XCTest, SwiftLint, Code Review 체크리스트 사용"
```

---

## 카테고리 2️⃣: 하네스 엔지니어링 (4장)

### Card 2.1
**[한글 앞면]**
```
Q. "하네스 엔지니어링"을 한 문장으로 정의하면?

답변 시간 목표: 20초
```

**[영어 뒷면 - 참고]**
```
정의:
Agent = Model (Claude) + Harness (검증)

하네스 = 입력 검증 + 출력 검증 + 폴백 + 품질 게이트

예시: AI 생성 코드 → SwiftLint → LLM Judge → XCTest → 머지
```

---

### Card 2.2
**[한글 앞면]**
```
Q. iOS 앱에서 AI 생성 코드를 프로덕션에 머지하기 전 
   4단계 품질 게이트는?

답변 시간 목표: 1분 30초
```

**[영어 뒷면 - 참고]**
```
4단계 게이트:

1️⃣ 문법 게이트 (10초, 자동)
   → swiftc --typecheck, SwiftLint

2️⃣ 패턴 게이트 (30초, LLM Judge)
   → "프로젝트 패턴과 일관성 있는가?"

3️⃣ 테스트 게이트 (5분, 자동)
   → XCTest 실행, 메모리 프로파일링

4️⃣ 보안 게이트 (10분, 수동)
   → Keychain, 암호화, 데이터 노출
```

---

### Card 2.3
**[한글 앞면]**
```
Q. LLM-as-a-Judge (AI 판사) 의 가장 큰 약점은 무엇인가?
   그리고 어떻게 보완하는가?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
약점: "AI는 코드가 실제로 실행되는지 확인할 수 없음"

보완 방법:
→ 반드시 XCTest를 함께 실행
→ AI는 "패턴/스타일" 검증만 (의미적 판단)
→ 실제 동작은 자동 테스트로 검증

신뢰도 높이기:
- 같은 코드를 5번 평가해서 표준편차 < 0.5 확인
- 정규화된 체크리스트 사용 (자유형 평가 X)
```

---

### Card 2.4
**[한글 앞면]**
```
Q. 네트워크 요청을 처리하는 ViewModel에 AI가 생성한 코드에서 
   흔한 3가지 버그는?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
흔한 3가지 버그:

❌ 버그 1: 타임아웃 없음
let (data, _) = try await URLSession.shared.data(for: request)
→ 수정: request.timeoutInterval = 15

❌ 버그 2: @MainActor 누락
Task {
    let result = try await api.fetch()
    self.data = result  // ← 크레시 가능
}
→ 수정: @MainActor func update()

❌ 버그 3: weak self 누락
api.fetch { [self] data in  // ← 메모리 누수
    self.data = data
}

→ 하네스로 감지: SwiftLint + Custom Rule
```

---

## 카테고리 3️⃣: 콤파운드 엔지니어링 (4장)

### Card 3.1
**[한글 앞면]**
```
Q. 콤파운드 엔지니어링의 4단계 루프를 순서대로 말하면?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
4단계:

1️⃣ PLAN (2시간)
   요구사항 분석 → 아키텍처 설계 → 프롬프트 준비

2️⃣ WORK (3시간)
   Claude Code가 90% → 개발자 마무리 → 테스트

3️⃣ REVIEW (2시간)
   14개 전문 에이전트 병렬 리뷰 (보안, 성능, 아키텍처 등)

4️⃣ COMPOUND (1.5시간) ← 중요!
   CHANGELOG 작성 → 솔루션 문서화 → CLAUDE.md 업데이트
   → 다음 PR은 더 빨라짐 (지수 함수적 가속)
```

---

### Card 3.2
**[한글 앞면]**
```
Q. 콤파운드 엔지니어링을 적용한 측정 가능한 
   3가지 성과는?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
측정 가능한 성과:

📊 속도 (개발 시간)
   - 1주일째: PR당 5시간
   - 4주일째: PR당 2.5시간
   - 12주일째: PR당 1시간 (5배 가속)

🐛 품질 (버그율)
   - 주당 2-3개 → 0.5개 (60% 감소)

💰 비용 (토큰)
   - PR당 $50 → $15 (70% 절감)
   - 이유: AI 가이드만 명확해서 토큰 적게 씀
```

---

### Card 3.3
**[한글 앞면]**
```
Q. Every.io 팀이 5개 프로덕트를 개발자 1명/프로덕트로 관리한다고 했는데,
   보통은 몇 명이 필요한가?

답변 시간 목표: 20초
```

**[영어 뒷면 - 참고]**
```
기존: 3명 개발자 / 프로덕트

콤파운드로: 1명 / 프로덕트 (3배 효율화)

조건:
✓ CLAUDE.md 잘 작성
✓ 솔루션 문서 체계적으로 기록
✓ 자동 테스트 잘 짜인 상태

한국 스타트업에 중요: 
→ 개발 팀 규모 1/3 축소 + 속도 2배 = 경쟁력
```

---

### Card 3.4
**[한글 앞면]**
```
Q. CLAUDE.md 파일에는 iOS 개발자로서 어떤 규칙을 써야 하는가?
   3가지 예를 들면?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
예시 규칙:

1️⃣ 아키텍처
"MVVM + Coordinator 패턴 필수
@StateObject (Observable) 사용 (deprecated EnvironmentObject 금지)"

2️⃣ SwiftUI
"NavigationStack 사용 (deprecated NavigationView 금지)
.task(id:) 사용으로 의존성 추적"

3️⃣ 메모리
"weak self 무조건 사용 (클로저 내)
순환 참조 의심: retain cycle 검사 필수"

효과: AI가 실수 반복하지 않음 → 리뷰 시간 50% 단축
```

---

## 카테고리 4️⃣: 프롬프트 엔지니어링 (4장)

### Card 4.1
**[한글 앞면]**
```
Q. Zero-Shot, Few-Shot, Chain-of-Thought 프롬프팅을 
   한 문장씩 설명하면?

답변 시간 목표: 45초
```

**[영어 뒷면 - 참고]**
```
정의:

🔵 Zero-Shot: 예시 없이 "해줄래?" 
   → 빠르지만 부정확 (정확도 70%)

🟢 Few-Shot: 예시 2-5개 제공 후 같은 패턴 요청
   → 패턴 일관성 90%+

🟡 Chain-of-Thought: "단계별로 생각하면서 코드 작성"
   → 복잡 로직에서 정확도 95%+

선택: SwiftUI View (Few-Shot) vs 금융 계산 (CoT)
```

---

### Card 4.2
**[한글 앞면]**
```
Q. 구조화된 출력 (Structured Output) 으로 
   AI가 생성한 JSON을 검증 가능하게 하는 
   iOS 예시를 코드로 보여줄 수 있는가?

답변 시간 목표: 2분 (코드 작성)
```

**[영어 뒷면 - 참고]**
```swift
// 1단계: 구조 정의
struct ValidationResponse: Codable {
    let isValid: Bool
    let score: Int // 0-100
    let issues: [Issue]
}

// 2단계: AI 프롬프트에서 JSON 형식 명시
let prompt = """
코드를 검증하고 JSON만 응답:
{"isValid": bool, "score": int, "issues": [...]}
"""

// 3단계: 자동 파싱 & 검증
let validation = try JSONDecoder().decode(
    ValidationResponse.self,
    from: response.data(using: .utf8)!
)

if validation.score < 80 { /* FAIL */ }

장점: 파싱 에러 없음, 타입 안전, 토큰 효율적
```

---

### Card 4.3
**[한글 앞면]**
```
Q. CLAUDE.md (시스템 프롬프트)와 개별 요청 (사용자 프롬프트)의 
   역할 분담은?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
역할 분담:

📄 CLAUDE.md (시스템 프롬프트) - 장기 규칙
- 아키텍처 패턴 (MVVM, weak self 규칙)
- 금지 항목 (deprecated APIs)
- 자동 검증 기준

💬 사용자 프롬프트 - 일회성 지시
- "이 기능을 구현해줄래?"
- "이 에러를 고쳐줄래?"
- Few-Shot 예시

효과: CLAUDE.md가 있으면 매번 설명할 필요 없음
→ 프롬프트 복잡도 30% 감소
```

---

### Card 4.4
**[한글 앞면]**
```
Q. iOS에서 Few-Shot 프롬프팅을 사용할 때, 
   제공하는 예시 코드의 "좋은 사이즈"는?

답변 시간 목표: 30초
```

**[영어 뒷면 - 참고]**
```
권장:

📏 코드 길이: 15-50줄
   → 너무 짧으면 패턴 안 보임
   → 너무 길면 토큰 낭비

📊 예시 개수: 2-3개
   → 1개: 부족
   → 5개+: 토큰 낭비

🎯 복잡도: 간단한 케이스
   → 기본 패턴을 먼저 보여주기
   → 엣지 케이스는 나중에

예시:
✓ 기본 UserListViewModel
✓ 네트워킹 추가된 버전
→ 이 패턴으로 new FeatureViewModel 생성
```

---

## 카테고리 5️⃣: Foundation Models (4장)

### Card 5.1
**[한글 앞면]**
```
Q. iOS 26의 Foundation Models 프레임워크에서 
   지원하는 디바이스 요구사항은?

답변 시간 목표: 30초
```

**[영어 뒷면 - 참고]**
```
지원 디바이스:

✓ iPhone 15 Pro 이상 (A17 Pro 이상)
✓ iPhone 16 (모든 모델)
✓ iPad (M1 칩 이상)
✓ Mac (Apple Silicon)

❌ iPhone 15 standard (불가)
❌ iPhone 14 이하 (불가)

체크코드:
let availability = SystemLanguageModel.default.availability
```

---

### Card 5.2
**[한글 앞면]**
```
Q. Apple의 온디바이스 Foundation Model (3B)이 할 수 있는 것 3가지와 
   못 하는 것 2가지를 말하면?

답변 시간 목표: 45초
```

**[영어 뒷면 - 참고]**
```
할 수 있음 (✓):
- 텍스트 요약
- 개체 추출 (이름, 위치, 숫자)
- 감정 분석
- 짧은 대화
- 창의적 텍스트 생성

못 함 (✗):
- 복잡한 추론 (계산, 로직)
- 코드 생성 (프로그래밍)
- 최신 정보 (2024년까지만 학습)

결론: 요약/분류는 온디바이스, 창의/코딩은 클라우드
```

---

### Card 5.3
**[한글 앞면]**
```
Q. Foundation Models가 사용 불가능한 사용자 
   (iPhone 15 standard, 구형)를 어떻게 처리하는가?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
우아한 폴백 (Graceful Fallback):

1️⃣ 온디바이스 시도
   → 성공하면 응답 (지연 <100ms)

2️⃣ 클라우드 폴백 (Claude API)
   → 실패하면 → 클라우드 호출 (지연 500-1000ms)
   → 비용 발생 ($3/M input, $15/M output)

3️⃣ 오프라인 폴백
   → 네트워크 없으면 → 사전 저장된 답변
   → 예: FAQ, 템플릿

코드:
try await tryOnDevice() ?? try await tryCloud() ?? getOfflineResponse()
```

---

### Card 5.4
**[한글 앞면]**
```
Q. 온디바이스 AI (Foundation Models)와 
   클라우드 AI (Claude API)를 언제 어떻게 선택하는가?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
선택 기준:

📍 온디바이스 (빠름, 저비용)
- 요약/분류
- 개인정보 민감 (건강, 금융)
- 오프라인 필수
- 지연시간 <200ms

☁️ 클라우드 (강함, 비쌈)
- 창의 쓰기
- 코딩 생성
- 복잡 추론
- 최신 정보 필요

💡 혼합 전략 (최적):
- 온디바이스로 빠른 응답
- 백그라운드에서 클라우드로 더 정확한 답변
- 사용자에게 선택지 제공
```

---

## 카테고리 6️⃣: 바이브 코딩 (4장)

### Card 6.1
**[한글 앞면]**
```
Q. "바이브 코딩"의 정의와 iOS 개발에서의 3가지 위험을 말하면?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
정의: AI에 명령 → 코드 복사-붙여넣기 → 실행 (이해 없음)

iOS 특화 위험:

⚠️ 메모리 누수
weak self 누락 → UIViewController 메모리 못 해제
→ RAM 부족 → 크래시

⚠️⚠️ 동시성 버그
@MainActor 누락 → Task 캔슬 미처리
→ 난수처럼 크래시 (재현 어려움)

⚠️⚠️⚠️ 보안 취약점
API 키 평문 저장 → 배포 후 보안 사고

해결: 자동 검증 + 코드 리뷰 필수
```

---

### Card 6.2
**[한글 앞면]**
```
Q. "책임감 있는 AI 개발"의 70/30 Rule이란?

답변 시간 목표: 30초
```

**[영어 뒷면 - 참고]**
```
70/30 Rule:

전체 작업 시간 = 100%

AI가 하는 일: 70%
- 보일러플레이트
- 테스트 스켈레톤
- 문서 초안

인간이 하는 일: 30%
- 아키텍처 결정
- 보안 검증
- 엣지 케이스 처리
- 최종 리뷰

⚠️ 잘못된 비율:
- AI 95% (무책임)
- 수동 5% (비효율)

✓ 올바른 비율:
- AI 70% → 인간 30% (균형)
```

---

### Card 6.3
**[한글 앞면]**
```
Q. 코드 일관성을 보장하는 3가지 방법을 말하면?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
3가지 방법:

1️⃣ Pattern Library 제시
   AI에게 기존 코드를 "참고 예시"로 줌
   → 일관성 90%+ 향상

2️⃣ SwiftLint + 자동 검사
   구조/스타일 자동 검증
   → 200줄 코드 5분 → 30초

3️⃣ Code Review 체크리스트
   ✓ 패턴 일관성
   ✓ 메모리 안전
   ✓ 동시성 올바름
   ✓ 에러 처리 완전성

효과: AI 생성 코드의 신뢰도 95%+
```

---

### Card 6.4
**[한글 앞면]**
```
Q. 면접관이 "당신의 GitHub 코드를 봤는데, 
   View 구조가 일관성이 없다. 왜인가?"라고 물으면?

답변 시간 목표: 1분 30초
```

**[영어 뒷면 - 참고]**
```
모범 답변:

"좋은 지적입니다.

초반에는 AI 출력을 많이 썼는데, 
이제는 3가지로 일관성을 보장합니다:

1. Pattern Library:
   기존 UserProfileView를 AI에게 제시
   → "이 패턴으로 SearchResultView 만들어줄래?"

2. Automated Checks:
   SwiftLint + 커스텀 룰
   → @StateObject 필수, weak self 검사

3. Code Review:
   체크리스트로 수동 검증

결과: 속도(70% 빠름) + 품질(버그율 동일) 동시 달성"

→ 면접관이 "데이터 있나?"라면 GitHub 링크 제시
```

---

## 카테고리 7️⃣: 멀티-에이전트 (2장)

### Card 7.1
**[한글 앞면]**
```
Q. 아키텍트-워커 패턴이란 무엇인가?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
패턴 구조:

        아키텍트 에이전트
        (큰 그림 설계)
                |
        ┌───┬───┼───┬────┐
        ↓   ↓   ↓   ↓    ↓
       워커1 워커2 워커3 워커4 워커5
      (Model)(API)(UI)(State)(Error)

아키텍트: 작업 분해 + 조율 + 최종 검증
워커: 각자 영역 담당 + API Contract 준수

통신: API Contract (단일 진실 공급원)

예시: 결제 기능 추가
→ 아키텍트가 5개 태스크로 분해
→ 각 워커가 독립적으로 구현
→ 최후 통합
```

---

### Card 7.2
**[한글 앞면]**
```
Q. 네이버/카카오가 2026년에 멀티-에이전트 플랫폼을 출시한다고 했는데, 
   iOS 개발자가 배워야 할 것은?

답변 시간 목표: 1분
```

**[영어 뒷면 - 참고]**
```
2026년 트렌드:

네이버:
- 쇼핑 AI + 여행 AI + 금융 AI
- 한 사용자 → 여러 에이전트 상호작용

카카오:
- "Kanana" 멀티-에이전트 플랫폼
- 전자상거래 + 금융 + 생활 서비스

iOS 개발자 요구사항:
1. 에이전트 간 데이터 흐름 (파이프라인)
2. 비동기 에이전트 호출 (async/await 심화)
3. 실패 시 폴백 전략
4. 개인정보 보호 (에이전트 간 데이터 교환)

준비: async/await 깊이 있게 공부 + 
      에이전트 API 설계 경험
```

---

## 카테고리 8️⃣: 면접 상황별 답변 (3장)

### Card 8.1
**[한글 앞면]**
```
Q. 면접 오프닝 질문:
   "당신이 AI와 함께 일할 때의 장점은?"

답변 시간 목표: 2분
```

**[영어 뒷면 - 참고]**
```
구조화된 답변:

"저는 AI와 인간의 강점을 결합합니다:

1️⃣ 속도 (생산성 3배)
AI가 보일러플레이트 70% → 1시간 → 15분

2️⃣ 품질 (버그 60% 감소)
SwiftLint + 자동 테스트 + 수동 리뷰

3️⃣ 축적 (지수 함수적 가속)
매 PR마다 배워서 다음은 더 빠름

증거:
- docs/solutions/ 폴더 (반복 패턴 기록)
- CLAUDE.md (팀 규칙 체계화)
- git 커밋 메시지 (배운 점)

결과:
한 명이 여러 프로젝트 관리 가능 + 토큰 비용 70% 절감"
```

---

### Card 8.2
**[한글 앞면]**
```
Q. 네이버 면접에서:
   "온디바이스 AI와 클라우드 AI를 어떻게 조합하겠는가?"

답변 시간 목표: 2분
```

**[영어 뒷면 - 참고]**
```
전략:

사용 사례 (쇼핑 추천):
┌─────────────────────────────┐
│ 사용자 입력 (검색어)         │
└────────────┬────────────────┘
             ↓
   ┌────────────────────────┐
   │ 온디바이스 (빠른 응답)  │
   │ 5초 → 기본 추천        │
   │ (요약/분류)            │
   └────────────┬───────────┘
                ↓
   ┌────────────────────────┐
   │ 백그라운드 클라우드     │
   │ (더 정확한 추천)        │
   │ 15초 (비용 발생)       │
   └────────────┬───────────┘
                ↓
   ┌────────────────────────┐
   │ UI 업데이트            │
   │ "더 좋은 추천 찾음"    │
   └────────────────────────┘

장점:
- 사용자는 빠른 응답 경험 (5초)
- 정확성도 높음 (15초 후)
- 개인정보 보호 (기본은 온디바이스)

비용:
- 자주 쓰는 기능만 클라우드 (선택적)
```

---

### Card 8.3
**[한글 앞면]**
```
Q. 쿠팡 면접에서:
   "AI 토큰 비용을 70% 줄이는 구체적인 방법은?"

답변 시간 목표: 1분 30초
```

**[영어 뒷면 - 참고]**
```
3가지 방법:

1️⃣ 콤파운드 엔지니어링 (40% 절감)
   매 PR 후 솔루션 문서화 + CLAUDE.md 업데이트
   → 2-10번째 PR부터 AI 가이드만 명확
   → 입력 토큰 50% 감소

2️⃣ Few-Shot 프롬프팅 (20% 절감)
   예시 코드 제시 → 명확한 출력
   → 재생성 요청 줄어듦

3️⃣ 구조화된 출력 (10% 절감)
   JSON 형식 지정 → 파싱 에러 제거
   → 재작성 요청 없음

측정:
ccusage --project ai-study 로 비용 추적
→ 4주일 후: $500 → $150

한국 스타트업에 중요: 
→ 개발 비용 1/3 (AWS + Claude 합쳤을 때)
```

---

## 🎯 최종 복습 (5분)

### 핵심 개념 5가지
```
1. 하네스 = AI 신뢰도 보장 (자동 검증 + 폴백)
2. 콤파운드 = 지수 함수적 가속 (매 PR 배움)
3. Few-Shot = 일관성 90%+ (예시 제시)
4. Foundation Models = 온디바이스 (프라이버시)
5. 바이브 코딩 = 위험 (책임감 있는 AI 개발)
```

### 면접 1분 자기소개 템플릿
```
"저는 AI와 인간이 함께 일하는 iOS 개발자입니다.

Claude Code로 주간 70% 작업을 5배 빠르게,
자동 검증으로 버그 60% 줄였고,
매 PR 배워서 다음은 더 빨라집니다.
(증거: GitHub - docs/solutions/, CLAUDE.md)

네이버/카카오의 멀티-에이전트 시대에는
이런 개발 문화가 팀 생산성 3배를 만듭니다."

(약 30초 - 인상적이고 구체적)
```

---

**팁**: 이 플래시카드를 매일 아침 5분씩 읽고, 
각 카드마다 "종이에 손으로 한국어 답변 써보기" 연습하기.
면접관이 가장 좋아하는 것은 "준비된 사람"입니다.

**화이팅! 🚀**
