---
date: 2026-04-12
sprint: "허브-워커 양방향 모델 완성 + UI 개선 + Tokenomics + validator 10/10 완료"
commits: 25+
journals: 6 (011, 012, 013, 014, 015, 017)
merged_prs: 22+ (ai-study 12 + moneyflow 6 + tarosaju 4)
accidents: 0
---

# 2026-04-12 회고 — 허브가 수집자가 되는 날

## 이번에 한 것

**핵심 테마**: 허브(ai-study)를 *관찰자*에서 *작업 공간 + 수집자*로 업그레이드. 3 프로젝트 모두 자동 머지 흐름으로 통일. 13 사이클 연속 사고 재발률 0 유지.

### Journal 011 — 허브-워커 동시 세션 안전 셋업

사용자 트리거: "허브가 워커 분석하면서도 워커 세션 작업을 안 깨뜨리게 셋업해놔".

- `/projects-sync` 슬래시 커맨드 신설 — 허브가 양쪽 워커를 read-only로 한 번에 진단 (fetch + status + ahead/behind + worktree 잔여물 + 최근 PR + 다른 세션 흔적)
- tarosaju에 `ai-review.yml` 이식 (박제 전이성 3회째) — chicken-and-egg에서 re-run 통해 자기 자신 머지
- 양쪽 워커 CLAUDE.md에 "동시 세션 안전 규칙" 4개 섹션 박제
- moneyflow의 **stale rule** 발견 → 정리: "PR 날리지 말고 메인 직접 머지"는 ai-review.yml 도입 후 PR #91~#95가 전부 봇 PR로 머지된 증거로 사문화

### Journal 012 — moneyflow market_analyst 런타임 검증 (Layer 1+2)

- 첫 탐색에서 발견: moneyflow에 **zod 의존성 없음**
- `/projects-sync`로 다른 세션 **7 active worktree + Conductor la-paz** 감지 → package-lock.json 동시 편집 충돌 위험
- 두 맥락 결합 → **순수 TypeScript type guard**로 Layer 1+2 구현 (zod 도입 대신)
- `parseJSON`에 optional validator 매개변수 추가 (backward compat)
- vitest 14 테스트 추가, 1m36s 자동 머지

### Journal 013 — 3 analyst 확장 (news/sentiment/fundamentals)

- Journal 012 패턴 1:1 복제. `checkAgentReportBase` 함수로 4 validator가 base 체크 공유
- vitest 24 테스트 추가, ~1m30s 자동 머지
- **13 에이전트 진행률 4/13 = 31%**

### Journal 014 — 허브에 ai-review.yml 이식 + 인바운드 tips PR 흐름

사용자 트리거: "각 프로젝트가 발견한 꿀팁을 너한테 PR로 쏘면 너가 하나의 엔트리로 관리". 직전에 사용자가 PR #15로 `dev-setup-tips-log.mdx` 생성 후 연장.

- PR #15 수동 머지 (사용자가 만든 PR)
- **moneyflow → ai-study 허브 이식** — ai-review.yml 복사, Test Gate만 조정 (vitest → npm test, env placeholder 제거)
- `dev-setup-tips-log.mdx`에 워커 → 허브 PR 제작 5단계 + 허브 처리 규칙 섹션 추가
- **예상 밖 완전 자동 dogfooding**: 첫 시도에 chicken-and-egg 건너뜀 (권한이 이미 설정돼 있었음), PR #18 → 1m6s 자동 머지
- 박제 전이성 **5회째** (전이 방향: moneyflow → ai-study, 허브가 처음 수용자 역할)

### 부가 작업

- **Vercel Analytics 연동** (ai-study): 사용자 "너도 vercel analytics 연동좀" → @vercel/analytics 설치 + layout.tsx에 `<Analytics />` → PR #19 (**첫 완전 자동 PR** — Journal 014 이후 첫 push 이벤트 → 1m5s 자동 머지)
- **워커 CLAUDE.md에 tips/ PR 패턴 안내** (moneyflow #100 + tarosaju #8): 각 워커에 워커→허브 tips PR 제작 5단계 박제. 양쪽 다 자동 머지.

## 잘된 것

1. **허브-워커 모델이 *실전에서* 작동 검증** — Journal 011 셋업 후 Journal 012/013/014 전부 동시 세션(moneyflow 워커 7 active worktree + Conductor)과 충돌 0회. 사고 재발률 0/13 유지.
2. **`/projects-sync`가 도구 선택의 *실시간 제약*이 됨** — Journal 012에서 zod 대신 type guard 결정의 근거가 됐다. 관찰 → 결정 → 실행 사이클이 한 세션 안에서 작동.
3. **첫 완전 자동 PR** — ai-study의 Vercel Analytics PR이 사용자 개입 0으로 1m5s에 landing. 양방향 기여 인프라가 *실제로* 작동함을 증명.
4. **한 사이클 한 박제 원칙 유지** — 각 Journal이 한 PR(또는 여러 PR을 한 사이클로 묶은 경우도 명시)로 정리. 인지 부하 단위가 일관.
5. **stale rule 자동 감지** — Journal 011 작업 중 moneyflow CLAUDE.md의 옛 규칙이 *실행 흔적*(PR #91~#95)과 모순됨을 발견하고 정리. 박제의 복리 효과가 *메타 수준*에서 작동.
6. **다른 세션 병행 작업 인지** — moneyflow #99 (다른 세션 compound), tarosaju #7 (다른 세션 안전성 8차). 내가 작업하는 동안 워커 세션들이 각자 작업 + compound까지 돌렸는데 내 PR들과 충돌 0.

## 아쉬운 것

1. **Node.js 20 deprecation 경고** — 모든 ai-review.yml 실행에 annotation. 2026-06-02까지는 유효하지만 언젠가 Node 24로 업그레이드 필요. workflow 파일에 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` 환경변수만 추가해도 됨 — 단 3 레포 모두 (moneyflow + tarosaju + ai-study).
2. **ci.yml + ai-review.yml 중복 Test Gate** — ai-study에서 한 PR에 두 실행이 돌아서 리소스 *잠재* 낭비. 아직 데이터 수집 전이라 중단 판단은 보류.
3. **Journal 014에서 계획 초과** — 원래 NEXT.md 큐엔 없던 작업이 사용자 트리거로 끼어들어 Journal 013 → 014 사이에 추가 사이클이 들어감. 계획 정확도 ~60%는 여전히 유지되지만 큐 재정렬을 매 사이클 append해야 하는 비용.
4. **moneyflow에서 worktree 잔여물 누적** — `.claude/worktrees/agent-*` 7개 중 4 prunable, 3 active. 다른 세션들이 정리 안 하고 있음. 내 스코프 밖이지만 장기적으로는 해결 필요.
5. **Journal 015 Zod 결정 미룸** — 9 debate 에이전트 응답 구조 복잡도 조사 없이 이번 세션 정지. 다음 세션에서 조사부터.

## 다음에 적용할 것

1. **Journal 015 착수 전 `/projects-sync` + 응답 구조 조사** — 9 debate 에이전트가 실제로 어떤 shape을 반환하는지 먼저 읽고, 그 복잡도를 본 후 zod 도입 여부 결정. 결정을 "제약 없이" 내리지 말고 *허브가 관찰한 워커 상태*를 제약으로 삼기.
2. **Node 20 deprecation 환경변수 일괄 적용** — 3 ai-review.yml에 `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true` 한 줄 추가. 한 사이클로 묶어서 3 PR 자동 머지 가능 (5분 사이클).
3. **Layer 3-4 (retry + instruction augmentation) 설계** — `callAI`에 `maxRetries` 옵션 추가는 13 에이전트 전체에 영향. Journal 016에서 다룰 때는 analyst 4 + debate 9 validator가 *이미 도입된 후*여야 한다. 순서: 015(9 debate) → 016(retry augmentation).

## Compound Assets (이번 세션 산출)

### 박제(허브 ai-study)

- `content/harness-engineering/harness-journal-011-concurrent-session-safety.mdx`
- `content/harness-engineering/harness-journal-012-market-analyst-runtime-validation.mdx`
- `content/harness-engineering/harness-journal-013-three-analysts-runtime-validation.mdx`
- `content/harness-engineering/harness-journal-014-hub-auto-merge-inbound-tips.mdx`
- `content/harness-engineering/dev-setup-tips-log.mdx` (사용자 생성 + 내가 For AI Agents 섹션 보강)
- `.claude/commands/projects-sync.md` (신설)
- `.github/workflows/ai-review.yml` (moneyflow → ai-study 이식)
- `NEXT.md` 갱신 로그 4개 append

### 박제(워커 moneyflow)

- `src/lib/trading/schemas/analyst-schemas.ts` — 4 validator (market + news + sentiment + fundamentals) + `checkAgentReportBase` 공유
- `src/lib/trading/ai-client.ts` — `parseJSON` optional validator 매개변수
- `src/lib/trading/agents/market-analyst.ts` + `news-analyst.ts` + `sentiment-analyst.ts` + `fundamentals-analyst.ts` — validator 적용 (각 1줄)
- `src/__tests__/market-analyst-schema.test.ts` + `analyst-validators.test.ts` — vitest 38 테스트 (14 + 24)
- `CLAUDE.md` — 동시 세션 안전 규칙 + CI/CD stale rule 정리 + 환경 팁 허브 안내
- `.gitignore` — `.claude/worktrees/` `.claude/projects/`

### 박제(워커 tarosaju)

- `.github/workflows/ai-review.yml` (moneyflow 이식)
- `CLAUDE.md` — 동시 세션 안전 규칙 + 환경 팁 허브 안내
- `.gitignore` — 동일

### 이 세션의 고유 지표

- **사고 재발률**: 0 / 13 사이클 누적 (wt-branch 도입 이후 연속)
- **자동 머지 성공**: 11개 PR 중 10개 (사용자 수동 머지는 ai-study PR #14/#16/#17만)
- **첫 완전 자동 PR 달성**: Vercel Analytics PR #19 (ai-study, 1m5s)
- **박제 전이성 5회째 달성**: moneyflow → ai-study (허브 첫 수용자 역할)
- **자기 검증 구조 5회째 달성**: 허브-워커 모델 반대 방향으로 허브가 워커 기여 수신 인프라 구축

## 수치

| 항목 | 값 |
|---|---|
| 커밋 (ai-study) | 6 |
| 커밋 (moneyflow 통해 landed) | 4 |
| 커밋 (tarosaju 통해 landed) | 3 |
| 신규 엔트리(박제) | 5 (Journal 4 + dev-setup-tips-log) |
| 신규 슬래시 커맨드 | 1 (`/projects-sync`) |
| 신규 워크플로 | 2 (tarosaju + ai-study ai-review.yml) |
| 런타임 validator | 4 (market + news + sentiment + fundamentals) |
| 신규 vitest 테스트 | 38 |
| 평균 자동 머지 시간 | ~1m20s |
| 사고 발생 건수 | **0** |

## 다음 세션이 가장 먼저 볼 것

- `NEXT.md`의 가장 아래 갱신 로그 4개 (Journal 011~014 + Vercel Analytics + tips pointer)
- `content/harness-engineering/harness-journal-014-hub-auto-merge-inbound-tips.mdx` (양방향 모델 완성 지점)
- `docs/solutions/workflow/2026-04-12-hub-worker-concurrent-session-safety.md` (이번 사이클의 재발 방지 체크리스트)
- `content/harness-engineering/dev-setup-tips-log.mdx`의 For AI Agents 섹션 (인바운드 PR 흐름 사용법)

**정지 지점 메시지**: *"허브는 이제 관찰자가 아니라 작업 공간 + 수집자다. 다음 세션은 Journal 015(9 debate 에이전트 validator + Zod 도입 결정)로 이어가면 된다. 사이클 분할 원칙을 따르며 `/projects-sync` 결과를 제약으로 삼아 도구를 선택하라."*

---

# Wave 2 — UI 개선 + Tokenomics + Journal 015 + 세션 마무리

## Wave 2에서 한 것

### 네비게이션 중앙 검색 + 사이드바 4 그룹 아코디언

사용자: "글이 많아져서 보기가 힘들어지기 시작함 + harness/context/prompt는 AI 엔지니어링 방법론으로 뭉쳐서 서브메뉴로".

- Header 3 구역 flex: 로고 / 중앙 SearchTrigger / 우측 nav
- SearchDialog를 root layout으로 글로벌화 (wiki/홈 개별 마운트 제거 → 모든 페이지에서 검색 가능)
- `CATEGORY_GROUPS` (schema.ts): 4 그룹 × 카테고리 × 엔트리 2층 접이식

### Tokenomics 카테고리 신설

사용자: "클로드 토큰 절감 엔트리는 서브카테고리로 격상시켜줬으면 ... Tokenomics 이런 느낌으로 지속적인 연구 및 디벨롭 필요".

- 11번째 카테고리, teal #14b8a6
- `content/tokenomics/` 디렉토리 신설 + 엔트리 이동 (git mv)
- "프로덕트 적용 기준 4 조건" 섹션 엔트리에 박제 (즉시 적용은 4 조건 AND 만족 시에만)
- `generate-content-manifest.mjs`의 CATEGORIES 수동 동기화 필요 발견 → 주석으로 의무 명시

### Tokenomics 첫 2건 적용

1. **Node 24 opt-in** (3 프로젝트 ai-review.yml): `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`. annotation 메시지가 "forced to run on Node.js 24"로 전환 확인.
2. **`.claudeignore` 신설**: `src/generated/`, `package-lock.json`, `.next/`, `docs/retros/`, `docs/solutions/` 등 불필요 context 배제. Claude Code가 매 턴 읽을 필요 없는 파일을 구조적으로 제외.

### Journal 015 — Trader 런타임 검증 (debate 9 첫 1개)

- Zod 도입 재평가 2차: `/projects-sync` 결과 moneyflow 상태 동일 → type guard 유지
- `validateTraderProposal`: TraderProposal은 AgentReport 확장 안 함 → `checkAgentReportBase` 재사용 불가 → 독립 체크
- vitest 14 테스트 추가
- 13 에이전트 진행 5/13 = 38%

### 체크아웃 함정 tips

- `dev-setup-tips-log.mdx`에 `git pull --ff-only` 실패 패턴 append (2회 재발 기록)
- 원인: 다른 세션이 feature 브랜치로 local checkout해둔 상태에서 pull

### MDX JSX 파싱 함정 2개 발견

- Journal 015 작성 시 `<3으로`가 JSX 태그 시작으로 파싱 → compile error
- 메모리 `project_ai_generated_mdx_guards.md`에 패턴 3(인라인 {}) + 4(`<숫자`) 추가

## Wave 2 잘된 것

1. **사용자 요청 → 즉시 반영 사이클** — "검색 만들어줘" → 1m1s 자동 머지, "Tokenomics로 격상" → 1m0s 자동 머지, "Vercel Pro 결제했으니 analytics" → 이미 연동 + Tokenomics 적용 기준 시스템으로 흡수
2. **ai-review.yml 리듬 안정화** — 이번 wave만 7+ PR이 자동 머지. 평균 ~1m. 수동 개입 0 (MDX 에러 수정 후 재push 1건 제외)
3. **Tokenomics "4 조건" 시스템의 첫 실전** — Node 24 opt-in + `.claudeignore` 2건이 기준 통과 후 적용. 적용 전에 "이거 안전한가?" 판단 프레임워크가 작동
4. **MDX 함정 패턴 누적** — 이전 2 + 이번 2 = 4 패턴 박제. 생성 파이프라인 프롬프트 가드에 반영 필요성 재확인
5. **다른 세션 병행 투명 작동** — moneyflow에서 다른 세션이 v0.9.32~34 + compound 3 사이클을 돌리는 동안 내 PR(Node 24, trader validator)이 충돌 0으로 자동 머지

## Wave 2 아쉬운 것

1. **세션 길이 과다** — 한 세션에 5 Journal + UI 개선 + Tokenomics + compound 2회는 많다. 컨텍스트 압축이 빈번해지면서 이전 작업의 디테일을 놓칠 위험. *다음 세션은 2-3 Journal 단위로 끊고 compound가 바람직*.
2. **MDX 에러 발견 지연** — Journal 015에서 `<3으로`가 자동 머지 실패 → 수정 → 재시도 = 불필요한 2 push. 작성 시 `{`, `<숫자` 패턴을 사전 grep으로 잡았으면 1 push에 통과.
3. **Vercel Pro 커스텀 이벤트 미착수** — 사용자 요청이 있었으나 compound + Tokenomics 적용으로 밀림. 다음 세션 큐 1번.
4. **moneyflow worktree 잔여물 해소 안 됨** — 여전히 7 worktree (4 prunable). 다른 세션 스코프이지만 허브에서 cleanup 제안 가능한 시점.

## Wave 2 다음에 적용할 것

1. **MDX 작성 시 사전 grep** — commit 전에 `grep -n '<[0-9]\|{[a-z0-9_]' file.mdx`로 JSX 파싱 함정 검출. 또는 `validate-content.mjs`에 이 패턴 사전 탐지 추가 (MDX compile 실패 전에 경고).
2. **세션 단위 축소** — 2-3 Journal + compound로 한 세션 완결. 5 Journal은 예외적 상황.
3. **Vercel Pro 커스텀 이벤트** — 다음 세션 큐 1번. `@vercel/speed-insights` 추가 + `track()` 커스텀 이벤트 (검색/카테고리/Quiz/Graph/Admin).
4. **Tokenomics 적용 로그 첫 기록** — `.claudeignore` + Node 24 opt-in 2건을 카탈로그 엔트리의 "적용 로그" 섹션에 append.

## Wave 2 수치 (이전 wave와 합산)

| 항목 | Wave 1 | Wave 2 | 합산 |
|---|---|---|---|
| Journal 박제 | 4 (011~014) | 1 (015) | **5** |
| 머지 PR | 13 | 9+ | **22+** |
| 자동 머지 | 10 | 8+ | **18+** |
| vitest 테스트 추가 | 38 | 14 | **52** |
| Runtime validator | 4/13 | +1(trader) | **5/13** |
| Tokenomics 적용 | 0 | 2 | **2** |
| MDX 패턴 박제 | 2(기존) | +2 | **4** |
| 사고 재발률 | 0/11 | 0/14 | **0/14** |

## Wave 2 다음 세션이 가장 먼저 볼 것

1. `NEXT.md`의 가장 아래 갱신 로그 (최종 세션 상태)
2. 이 회고록의 Wave 2 "다음에 적용할 것" 4개
3. `content/tokenomics/claude-code-token-levers-catalog.mdx`의 "프로덕트 적용 기준" + "적용 로그"
4. Task #23 (pending): Vercel Pro Analytics 커스텀 이벤트

**최종 정지 지점 메시지**: *"이번 세션은 허브-워커 모델의 설계(011) → 실전 사용(012-015) → 양방향 확장(014) → UI/Tokenomics 인프라(22-27)까지 한 호흡에 가져갔다. 다음 세션은 Vercel Pro 커스텀 이벤트(사용자 미완 요청)부터 시작하고, validator 016(investment_judge + devils_advocate)으로 이어가면 된다. 세션 길이는 2-3 Journal 단위로 끊을 것."*

---

# Wave 3 — Journal 017 + 사이드바 서브그룹 + 10/10 에이전트 완료

## Wave 3에서 한 것

### Journal 017 — 10/10 에이전트 런타임 검증 완료

- 다른 세션이 investment_judge(PR #106) 이미 완료한 것 발견 → devils_advocate부터 시작
- **devils_advocate**: LLM raw output에 validator 적용하는 새 패턴 (parseJSON → 후처리 사이에). `DevilsAdvocateRawOutput` 별도 타입
- **research-debate**: bull/bear researcher의 `{ argument, counter_points }` flat structure
- **risk-debate**: 3 관점(aggressive/conservative/neutral) 중첩 구조. `checkRiskPerspective` 헬퍼로 코드 중복 제거
- **portfolio-manager**: 가장 복잡한 에이전트. `price_targets` 중첩 검증 포함
- moneyflow PR #107 (devils_advocate) 자동 머지, PR #108 (나머지 3개) rebase 충돌 해결 후 자동 머지
- **Zod 3차 재평가 → type guard 최종 확정**: 중첩 구조에서도 type guard가 충분

### 사이드바 Harness Journal 서브그룹

- harness-engineering 카테고리에서 Journal 17개를 접이식 서브그룹으로 분리
- 일반 글 14개가 먼저 보이도록, Journal은 기본 접힘
- Journal 페이지 보고 있으면 자동 펼침

### Journal 트리거 문구 정리

- J012, J015, J017에서 "쭉쭉 진행해줘" 같은 범용 진행 지시 제거
- 구체적 요청만 인용하는 기준 확립 → 메모리 `feedback_journal_trigger.md` 저장

## Wave 3 잘된 것

1. **10/10 에이전트 Layer 1+2 마일스톤 달성** — Journal 012에서 시작해 6개 사이클 만에 전체 에이전트(custom_prompt 제외) 런타임 검증 완료
2. **rebase 충돌 깔끔 해결** — devils_advocate PR이 먼저 머지되면서 remaining-validators PR과 충돌. 양쪽 코드 모두 유지하는 merge로 25/25 테스트 통과
3. **세션 길이 적절** — 1 Journal + 사이드바 개선 + 정리. Wave 2 피드백 "2-3 Journal 단위로 끊기" 적용

## Wave 3 수치

| 항목 | 이전 누적 | Wave 3 | 합산 |
|---|---|---|---|
| Journal 박제 | 5 | +1 (017) | **6** |
| moneyflow PR | 7 | +2 (#107, #108) | **9** |
| vitest 테스트 추가 | 52 | +40 | **92** |
| Runtime validator | 5/10 | +5 | **10/10** |
| 사고 재발률 | 0/14 | 0 | **0** |

**Wave 3 정지 지점**: *"Layer 1+2 완결. 다음은 Layer 3-4(retry + instruction augmentation) — callAI에 maxRetries 옵션 추가. Journal 018."*
