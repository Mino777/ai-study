# 2026-04-19 세션 16b 회고 — 긱뉴스 자동 큐레이션 + 허브 디스패치

## 이번에 한 것

1. **긱뉴스 자동 큐레이션 파이프라인** — curate-geeknews.mjs + auto-lesson-geeknews.yml
2. **실제 테스트** — 밀린 이슈 #52, #53으로 엔트리 2건 생성, PR #55 머지
3. **bot 오트리거 버그 수정** — generate-on-pick에 Bot 필터 추가
4. **Mermaid 에러 2건 수정** + Gemini 프롬프트 규칙 5 추가
5. **날짜 UTC→KST 통일** — generate-lesson.mjs + 워크플로우 4개
6. **이식 가능성 평가** — 18-19일 엔트리 6건 → 즉시 이식 3건 + 큐 2건
7. **허브 디스패치 체계** — hub-dispatch 라벨 + 이슈 6건 + 워커 3프로젝트 CLAUDE.md

## 잘된 것

- 긱뉴스 큐레이션 → 테스트 → 버그 발견 → 수정까지 한 사이클에 완료
- bot 오트리거 버그를 실제 사고(중복 PR #56, #57)에서 발견하여 즉시 수정
- 허브 디스패치 체계로 크로스 프로젝트 태스크 전달 문제 구조적 해결

## 아쉬운 것

- **aidy 프로젝트 누락 반복** — 이식 평가에서 aidy를 빠트려 유저가 다시 지적. 메모리에 이미 있었는데 적용 실패. 메모리를 강화했지만, 근본 원인은 "워커 프로젝트 나열" 시 무의식적으로 2개만 떠올리는 패턴 — 체크리스트화가 필요
- **워크플로우 cancel 타이밍 판단 실패** — `gh run cancel`을 보냈지만 이미 PR이 생성된 후라 중복 PR 발생. cancel 전에 run 상태를 먼저 확인했어야 함

## 다음에 적용할 것

- 워커 프로젝트 나열 시 "moneyflow, tarosaju, aidy" 3개 체크리스트 자체 점검
- GitHub Actions 워크플로우 cancel 전 `gh run view` 상태 확인

## Compound Assets

| 유형 | 파일 | 설명 |
|---|---|---|
| 스크립트 | `scripts/curate-geeknews.mjs` | 긱뉴스 RSS 큐레이션 + 이식 평가 |
| CI | `.github/workflows/auto-lesson-geeknews.yml` | 24h 미응답 자동 큐레이션 |
| 엔트리 | `content/agents/multi-claude-agent-management-compile-strategy.mdx` | 긱뉴스 자동 |
| 엔트리 | `content/backend-ai/ai-api-cost-prevention-serverless-security-pattern.mdx` | 긱뉴스 자동 |
| 엔트리 | `content/context-engineering/contexty-*.mdx` | PR #55 머지 |
| 프로세스 | hub-dispatch 라벨 + 이슈 6건 + 워커 CLAUDE.md 규칙 | 크로스 프로젝트 디스패치 |

## 수치

| 항목 | 값 |
|---|---|
| 커밋 수 | 12 (이 스프린트) + 5 (이전 compound) = 17 |
| 파일 변경 | 11 |
| 삽입/삭제 | +853 / -11 |
| 디스패치 이슈 | 6건 (4 프로젝트) |

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|---|---|---|
| bot 댓글이 generate-on-pick 오트리거 | user.type != Bot 필터 | generate-on-pick.yml |
| Gemini가 Mermaid `<br/>` 반복 생성 | 프롬프트 규칙 5 추가 | generate-lesson.mjs |
| 날짜가 UTC로 찍힘 | 전 파이프라인 KST 통일 | generate-lesson.mjs + 4 yml |
| 허브→워커 태스크 전달 수단 없음 | hub-dispatch 라벨 + CLAUDE.md 규칙 | 4 프로젝트 CLAUDE.md |
| aidy 프로젝트 누락 반복 | 피드백 메모리 강화 | memory/feedback_projects_sync_includes_aidy.md |

## Anti-Rationalization Guard

- "특이사항 없음" → 아쉬운 점 2건 (aidy 누락 + cancel 타이밍)
- 외부 귀인 점검: "cancel이 늦었다"가 아니라 "cancel 전 상태 확인 안 함"
- 테스트 없이 추정: auto-lesson-geeknews.yml의 자동 머지 로직은 실제 CI 환경에서 미검증 (로컬에서만 수동 테스트)

### 2차 방어선
이 세션 주요 커밋 17건 — `/cross-session-review` 실행 권장.
