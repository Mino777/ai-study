# 2026-04-17 Retro — 세션 6: Aidy Journal 006 박제 + 교훈→도구→사례 3단 연결

## 세션 요약

**키워드**: Aidy Session 7 박제 (WO-010 iOS CI self-hosted 전환) + 진단 3회 정정 프로세스 + 기존 엔트리 역링크 보강

**한 줄**: 세션 5에서 박제한 교훈(Journal 003 토큰 경제성)이 세션 5→6 사이 도구(v0.7.1 send-seq/429 backoff/ci-status.sh)로 진화하고, 그 도구의 첫 실행이 Journal 006 사건을 만들어낸 **3단 연결**을 박제 구조에도 반영했다.

---

## 이번에 한 것

### 1. Aidy Journal 006 박제 (117 → 118)

- `content/harness-engineering/aidy-journal-006-ios-ci-self-hosted-runner-migration.mdx`
- **진단 3회 정정 타임라인**을 그대로 박제:
  - t1 1차: tuist 4.180.0 (macOS 15 SDK) ↔ runner macos-14 미스매치 → exit 134
  - t2 정정1: 결제 차단(`payments have failed`) 별개 사유. "한도 초과 아님" ≠ "결제 정상"
  - t5 정정2: `test.yml` 만 self-hosted로 옮긴 직후 `ai-review.yml` 알림 폭탄. PR 1개 = 워크플로 1개 가정 깨짐
- **ADR-009 요약** + 대안 비교 테이블 (billing 정상화 / Xcode Cloud / disable / self-hosted)
- **실측 수치**: 124 tests / 1m 56s / 4월 macOS 분 ~180min → 0min
- **박제 교훈 4건** + **후속 WO 3건** (011 Swift 6 / 012 Node.js 24 P0 2026-06-02 강제 / 013 워크플로 통합)
- **Tokenomics 4 조건 대입**: self-hosted는 공식문서 + 실측 + 부작용 관측 + 롤백 용이(`runs-on` 한 줄) 모두 충족

### 2. 기존 엔트리 출처 보강 (교훈→도구→사례 3단 연결)

- `aidy-journal-003-parallel-dispatch-token-economics.mdx` 출처 섹션 —
  `a4f8861` v0.7.1 커밋 SHA 추가. 주석: "s6 교훈이 실제 도구로 박제된 시점" + Journal 006 역링크
- `tmux-flush-automation-pattern.mdx` 출처 섹션 —
  자매 도구 `ci-status.sh` 경로 추가 + 동일 커밋 + Journal 006 역링크
- **연결 구조**:
  ```
  Journal 003 (교훈: 토큰 경제성)
     │ 이 교훈을 도구로 박제 →
  a4f8861 v0.7.1 (send-seq + 429 backoff + ci-status.sh)
     │ 이 도구의 첫 실행이 발견 →
  Journal 006 (WO-010 iOS CI 사건)
  ```
  양 끝 엔트리에서 서로를 역링크. 체인이 닫힘.

### 3. NEXT.md 세션 6 종료 교체

- 완료 큐(Journal 006 박제 + 출처 보강) 제거
- 다음 순위 복귀: Layer 3 Phase 2b 다국어 임베딩 모델 비교
- 상태 스냅샷 갱신: 117 → 118 엔트리, Aidy Journal (000~**006**)
- 최근 갱신 로그 교체 (append 금지, 1개만 유지)

---

## 잘된 것

- **진단 정정 과정 자체를 박제** — ADR-009 에 "의사결정 기록 (시간순 — 진단의 진화)" 테이블이 있어서 Journal 006 에 그대로 옮겼다. 완결된 결정만 박제가 아니라 **진단이 틀렸던 과정** 이 다음 에이전트에게 가장 큰 교훈. "1차 진단으로 끝내기" 안티패턴을 Actionable Steps + Anti-patterns 양쪽에 박제
- **역링크 보강 최소 변경 (각 1~2줄)** — 기존 엔트리 전면 재작성이 아니라 "출처" 섹션에 한 줄 추가. Journal 003 이 9.9K 그대로 유지, tmux-flush 도 원형 유지. 체인이 닫히는 효과만 더함
- **NEXT.md 교체 운영 원칙 준수** — append 금지, 완료 즉시 제거, 최근 갱신 1개만. 지난 세션 종료 로그는 retros/CHANGELOG 로 이관됨
- **빌드 + 검증 먼저 → 커밋 → 푸시 순** — 118 entries 총합, Mermaid 30 블록 모두 통과. 기존 잔존 warning 1건(api-contract `{worker}`)은 NEXT.md #5 로 승계
- **토큰 효율** — 이번 세션은 aidy-architect 측 자료(HANDOFF + ADR-009 + Gate-2 review + WO-010 def + commit meta)를 한 번씩만 읽고 Journal 집필. 검색/재확인 반복 없음

## 아쉬운 것

- **aidy-architect Session 7 종료를 사용자 제보로 뒤늦게 감지** (세션 5 종료 후 NEXT.md에 기록됐었음) — 허브(ai-study)가 워커 진행 상황을 자동 감지하는 훅이 약함. `/projects-sync` 가 3 워커 fetch 는 해주지만 *새 ADR/커밋이 박제 대상인지* 판별은 사람에게 의존. N=2 추가 발생 시 validator 승격 후보
- **Tokenomics 4 조건 대입을 Journal 006 본문이 아닌 "박제 교훈 #4" 한 줄로만 언급** — self-hosted runner 결정은 Tokenomics 카탈로그에 프로덕트 적용 레버로 등재할 가치가 있다. 별도 `tokenomics/` 엔트리 또는 `claude-code-token-levers-catalog.mdx` 업데이트 후보
- **사용자 Mac 재부팅 검증 미완** — ADR-009 후속 검토 항목에 "launchd 자동 부팅 검증" 이 있는데 아직 실측 안 됨. WO-014 로 등록할지 검토 필요

## 다음에 적용할 것

1. **교훈→도구→사례 3단 연결이 감지되면 역링크 보강을 박제 사이클 기본 작업으로** — 새 Journal 집필 시 "이 사건의 원인이 된 교훈 엔트리가 있는가? 그 교훈이 도구화된 커밋이 있는가?" 체크리스트화. 메모리로 박제
2. **워커 프로젝트 ADR 등록 감지 훅 검토** — `/projects-sync` 결과를 `git log --grep="ADR-"` 로 필터링해 새 ADR 발견 시 알림. N=3 쌓이면 구현
3. **Tokenomics 카탈로그에 self-hosted runner 레버 등재** — 4 조건 대입 결과를 정식 엔트리로. Tier 1(즉시 적용) 후보

## Compound Assets

- **진단 정정 타임라인 포맷** (ADR-009 / Journal 006) — "t0 사실 | 진단 | 비고" 테이블. 진단이 1회로 끝나지 않는 인프라 문제에 그대로 재사용 가능. moneyflow/tarosaju 에서 유사 상황 발생 시 포맷만 복제
- **교훈→도구→사례 3단 연결 박제 패턴** — 재사용 가능한 박제 구조. 양 끝 엔트리의 "출처" 섹션에 서로를 링크. 허브(ai-study) 운영 방식으로 정착시킬 가치
- **`ci-status.sh` 자체** — gh CLI + jq 기반, 3 워커 동시 폴링, `--watch`/`--json`/`--since` 옵션. aidy 외 프로젝트로 복제 가능 (`WORKERS=(...)` 배열만 교체)
- **self-hosted runner 결정 체크리스트** (Journal 006 Actionable Steps) — macOS CI + 10x 가중치 + 사용자 Mac 가용 → 도입. 다른 팀이 iOS CI 비용 고민할 때 참조 가능

## 수치

| 항목 | 값 |
|---|---|
| 변경 파일 | 4 (Journal 006 신규 + Journal 003/tmux-flush 출처 보강 + NEXT.md) |
| 추가 라인 | +263 |
| 삭제 라인 | -65 (NEXT.md 완료 큐 제거 + 갱신 로그 교체) |
| 신규 엔트리 | 1 (Journal 006) |
| 수정 엔트리 | 2 (Journal 003 · tmux-flush) |
| 총 엔트리 수 | 117 → 118 |
| aidy 박제 대상 커밋 | 2 (`a4f8861` + `0e23007`) |
| 박제된 실측 수치 | 4월 macOS 분 ~180min → 0min, 124 tests / 1m 56s |
| 빌드 | ✅ 통과 |
| Mermaid 검증 | ✅ 30 블록, 0 에러 |
| 신규 warning | 0 (기존 1건 잔존, NEXT.md 큐) |
| 커밋 | 1 (`50be052`) |

---

## 메타 관찰 — 허브-워커 박제 흐름의 새 조각

세션 5에서 워커의 Session 5/6 결과물 10건을 한 번에 박제했다. 세션 6는 그 사이에 워커가 스스로 Session 7을 완주하고 끝낸 상태를 허브가 따라잡은 케이스다.

**허브가 따라잡을 때의 핵심**: 워커의 *완결된 결정*(ADR-009)만이 아니라 *그 결정까지의 진단 진화*를 놓치지 말 것. 완결본만 읽고 박제하면 "왜 1차 진단이 틀렸는가"가 증발한다. 다음 에이전트가 같은 1차 진단 함정에 빠질 위험이 그대로 남음.

Journal 006 의 "의사결정 기록 (시간순)" 테이블이 이 교훈의 실제 적용.
