# Retro — 2026-04-19 Session 16i: 히든 이력서 페이지

## 이번에 한 것

- /resume 히든 페이지: R키 5번 이스터에그 → Resume 네비 메뉴 노출
- iOS Engineer / FDE(채널톡) 탭 스위처 구현
- 그린카 경력을 608건 실적 기반으로 전면 재작성
- Harness/Context/Compound 방법론을 프로젝트별로 면접관이 이해 가능한 수준으로 풀어씀
- FDE 전용 섹션: AI 비즈니스 문제 해결 경험 + 현장 문제 발견 경험
- PDF 다운로드, 프로필 사진, 기술블로그 링크
- 노션용 마크다운 2벌 (iOS + FDE) 생성

## 잘된 것

- **탭 구조**: 같은 UI 프레임에서 데이터만 교체하는 구조가 깔끔. 새 직무 추가 시 데이터 객체만 추가하면 됨
- **FDE 리프레이밍**: 같은 경험을 iOS(기술 깊이) vs FDE(비즈니스 임팩트) 두 관점으로 재구성한 것이 효과적
- **면접관 관점 피드백 반영**: HE-1, CE-6 같은 내부 코드 → 면접관이 바로 이해 가능한 서술로 전환
- **마우스 그라디언트 팔로워**: Brittany Chiang 참고, 시각적 차별화

## 아쉬운 것

- **Turbopack 캐시 문제로 시간 낭비**: dev 서버가 구버전을 계속 서빙. .next 삭제 + 하드 리프레시로도 안 되어 포트를 변경해서 해결. 원인 파악 미흡 — 다음엔 처음부터 `rm -rf .next` + 다른 포트로 시작하는 게 나았을 것
- **디자인 이터레이션 과다**: 3번 완전 재작성 (v1 기본형 → v2 타임라인 → v3 Brittany Chiang 스타일). 처음부터 레퍼런스를 먼저 리서치했어야 함
- **rtk hook 간섭**: `next dev`가 rtk에 의해 exit code 1로 죽는 문제. rtk가 에러 메시지를 삼켜서 디버깅에 시간 소요

## 다음에 적용할 것

- dev 서버 캐시 문제 발생 시: 즉시 포트 변경 (3001) + `.next` 삭제. 같은 포트에서 리트라이하지 않기
- 디자인 작업 시: 레퍼런스 리서치를 먼저 하고 한 번에 구현. 이터레이션은 미세 조정만
- 이력서 내용 변경 시: 노션 md 파일도 동기화 필요 (현재 iOS 노션 파일은 구버전 상태)

## Compound Assets

- `/resume` 페이지 + 탭 구조: 새 직무 탭 추가 시 데이터 객체만 추가
- 노션 마크다운 템플릿 2벌: Downloads/notion-resume.md, notion-resume-fde.md
- print CSS 패턴: `.site-header` 셀렉터로 사이트 헤더만 숨기고 페이지 내 `<header>` 보존

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| print CSS가 이력서 header까지 숨김 | `header` → `.site-header` 셀렉터 변경 | globals.css, header.tsx |
| Skills에서 CSS uppercase가 iOS→IOS 변환 | 카테고리 라벨에서 uppercase 제거 | resume/page.tsx |

## 다음 세션 후보

- 이력서 디자인 최종 폴리싱 (글자 크기/마진 미세 조정)
- 노션 이력서 동기화 (현재 코드와 노션 md 내용 불일치)
- FDE 탭에 "왜 FDE인가" 섹션 추가 검토
