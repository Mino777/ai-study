# 2026-04-14 회고

## 이번에 한 것

1. **LLM-First 위키 엔트리 6개 현행화** — ai-agent-start-here, llm-first-wiki-principles, compound-automation-slash-commands, skill-system-introduction, harness-journal-000-baseline, compound-engineering-philosophy. 모두 70 엔트리 / 7 슬래시 커맨드 / Journal 021 기준으로 갱신.
2. **iOS Journal 004/006 클래스명 익명화** — 도메인 특정 네이밍 12개 제거 (Oneway→ServiceA, AZoneHistoryEntity→AHistoryEntity 등)
3. **모바일 하단 탭에 저널 추가** — 5탭 (홈/저널/Wiki/대시보드/Vibe)
4. **STRATEGY.md 신설** — Gemini 외부 평가 기반 4대 무기 정의 + Tier별 우선순위 + 포트폴리오 워딩 가이드
5. **AI 과외 파이프라인 slug 근본 수정** — 한글 질문→영문 slug 자동 변환 + tags 정제

## 잘된 것

- **5 에이전트 병렬 투입**으로 위키 현행화 6개 파일을 한 번에 처리. 각 에이전트가 독립적으로 읽기→수정 완료. 충돌 0건.
- **근본 원인 추적**: 404 에러를 단순 rename이 아니라 파이프라인 코드(`generateCustom()`)까지 추적해서 구조적 수정. 재발 가능성 원천 차단.
- **STRATEGY.md**: 외부 평가를 즉시 문서화. 앞으로 "4대 무기 중 어느 것을 강화하는가?"로 우선순위 판단 가능.

## 아쉬운 것

- **AI 과외 파이프라인 slug 문제가 이제야 발견됨** — 커스텀 주제 생성은 처음부터 있던 기능인데, 그동안 번호 선택만 사용해서 한글 slug 문제가 잠복해 있었음.
- **위키 현행화가 밀려 있었음** — 36→70 엔트리로 2배 성장했는데 메타 문서들이 36 기준으로 멈춰 있었음. 정기적 갱신 트리거 필요.

## 다음에 적용할 것

- [ ] AI 과외 파이프라인 커스텀 주제로 실제 테스트 — 한글 질문 입력 → 영문 slug 정상 생성 확인
- [ ] 위키 현행화 주기 설정 — Journal 10편 단위 또는 엔트리 20개 증가 시 메타 문서 갱신
- [ ] STRATEGY.md Tier 1 항목 착수 (LLM-as-a-Judge 데이터 분석)

## Compound Assets

| 자산 | 위치 | 재사용 시점 |
|---|---|---|
| STRATEGY.md | `STRATEGY.md` | 새 기능/엔트리 우선순위 판단 시 |
| slug 근본 수정 | `scripts/generate-lesson.mjs` | 다음 커스텀 주제 생성 시 자동 적용 |
| 솔루션 문서 | `docs/solutions/ai-pipeline/2026-04-14-*` | AI 파이프라인 디버깅 시 |
| iOS 익명화 패턴 | Journal 004/006 | 향후 iOS Journal 작성 시 참조 |

## 수치

- 커밋: 5
- 파일 변경: 14
- 에이전트 병렬: 5 (위키 현행화)
- 위키 현행화: 6 엔트리
- 익명화: 12 네이밍
- 파이프라인 함수 추가: 2 (generateSlugFromTitle, titleToSlugFallback)
- 빌드 검증: 3회 (모두 통과)

---

## 추가 (밤 세션)

### Mermaid 렌더링 근본 수정

- **4회 시도 과정**: pre className(Shiki 덮어씀) → data-attr(MDX props 미전달) → div override(MDX 미적용) → DOM 직접 렌더(성공)
- **최종 구조**: rehypeMermaid(서버, Shiki 전) + MermaidRenderer(클라이언트, DOM 직접)
- **영향**: 18파일 22블록 전부 정상 렌더링

### frontend-ai 엔트리 박제

- `mdx-mermaid-shiki-coexistence.mdx` (265줄) — 4번의 삽질 과정 + 각 실패 이유 + 최종 해법

### 추가 수치

- Mermaid 수정 커밋: 4 (v1~v4)
- 브라우저 디버깅: 6회 (chrome JS 실행으로 React props / DOM 상태 확인)
- 엔트리: +1 (frontend-ai)

### validate-content JSX 탐지 + Tokenomics

- `detectJsxTraps()` — `{중괄호}`, `<숫자`, void 태그 3패턴 사전 경고. 72개 파일 전부 0건 (현재 클린)
- Gemini 프롬프트에 MDX 문법 제약 4항목 추가 — AI 생성 시점 방지
- ccusage 30일 베이스라인 2차: $4,316 / 7.33B tokens / cache 98.6%
- LLM-as-a-Judge 데이터 분석: BLOCKED (moneyflow 0건, tarosaju 테이블 미생성)

### 세션 최종 수치

- 커밋: 15+
- 파일 변경: 20+
- 신규 엔트리: 1
- 신규 인프라: 3 (rehypeMermaid, MermaidRenderer, detectJsxTraps)
- 빌드 검증: 8회 (모두 통과)
- 브라우저 디버깅: 6회
- Mermaid 수정 시도: 4 (v4 성공)
