# 2026-04-20 회고 — 세션 18: gma-ios 인프라 이식 + 이슈 정리

## 이번에 한 것

1. **gma-ios 인프라 분석** — CLAUDE.md, settings.json, hooks, agents, docs/solutions 전체 스캔
2. **4가지 패턴 이식** — includeGitInstructions, SessionStart hook, permissions.deny, 모델별 에이전트
3. **aidy-architect 에이전트 3개 추가** — architect-advisor(opus)/code-reviewer(sonnet)/researcher(haiku)
4. **GitHub 이슈 7건 생성** — NEXT.md 미완료 항목 전체 이슈화
5. **#63~#66 4건 즉시 해결** — grep 패턴 수정, Mermaid 0건 확인, applicable_to 정밀화, 배지 검증
6. **applicable_to 91개 재추론** — 단순 ["any"] → moneyflow/tarosaju/aidy 특화 74%

## 잘된 것

- **이식 후 즉시 실행**: gma-ios 분석 → 4가지 패턴 식별 → 동일 세션에서 전부 적용. 관찰과 행동 시차 0
- **이슈 생성 → 즉시 해결 루프**: 이슈 7건 만들고 4건을 같은 세션에서 클로즈. "나중에 할 것"이 아니라 "지금 되는 것"부터
- **#64 재발 없음 확인**: Mermaid 충돌 "63건" 이슈를 만들었지만 실측하니 0건. 이전 세션에서 해결됨. 실측 전 이슈 생성은 낭비지만 발견이 빠른 것은 좋았다

## 아쉬운 것

- **이슈 생성 전 실측 안 함**: #64 Mermaid 충돌은 NEXT.md의 오래된 메모 기반으로 이슈를 만들었다. 내가 실측 먼저 하면 불필요한 이슈 생성을 막을 수 있었다. **이슈 생성 전 현재 상태 확인을 건너뛴 판단 실수**.
- **applicable_to 측정 grep 첫 시도 실패**: `grep -v '"any"\]'` 패턴이 `["aidy","any"]`도 걸러냄. 간단한 테스트 케이스 하나만 확인했으면 방지됐다. **grep 역 필터 결과를 검증 없이 신뢰한 판단 실수**.

## 다음에 적용할 것

- **이슈 생성 전 현재 상태 실측**: NEXT.md의 수치를 그대로 이슈 본문에 쓰지 말고, 먼저 `node scripts/validate-content.mjs` 등으로 현재값 확인 후 생성
- **grep 역 필터(`-v`) 작성 시 테스트 케이스 확인**: 제외하려는 패턴과 보존하려는 패턴이 겹치지 않는지 echo 테스트 1개

## KPI 판정

| 지표 | baseline | target | actual | 달성률 | verdict |
|------|----------|--------|--------|--------|---------|
| 엔트리 수 | 144 | 150 | 146 | 33% | stagnant |
| AI Directive 커버리지 | 85% | 100% | 77% | -53%* | — |
| 자동화 워크플로우 수 | 5 | 7 | 10 | 250% | exceeded |
| applicable_to 정밀도 | 0% | 20% | 74% | 370% | exceeded |

*Directive: 분모 증가(신규 엔트리) 효과. 절대 수 113개는 증가. 다음 KPI는 절대 수로 정의.

**종합 verdict**: 인프라/품질 지표 exceeded, 콘텐츠 지표 stagnant → **action: 다음 세션 콘텐츠 생성 집중**

## 프로세스 개선

| 재료 | 개선 | 파일 |
|---|---|---|
| gma-ios 인프라 격차 | session-start.sh + permissions.deny 이식 | .claude/settings.json, hooks/ |
| Bash 훅 grep JSON 파싱 취약 | python3 json 파싱으로 교체 | .claude/settings.json |
| applicable_to 단순 추론 한계 | --refine 플래그 + moneyflow/tarosaju 감지 | scripts/backfill-frontmatter.mjs |
| shadow-benchmark CI 파싱 불일치 | 영문 summary 라인 추가 | scripts/shadow-benchmark.mjs |

## Compound Assets

- `docs/solutions/workflow/2026-04-20-cross-project-infra-porting.md` — 레포 간 .claude/ 인프라 이식 프로토콜
- `.claude/hooks/session-start.sh` — NEXT.md 연동 세션 컨텍스트 자동 표시
- `scripts/backfill-frontmatter.mjs` (--refine 확장) — 프로젝트 특화 applicable_to 재추론
