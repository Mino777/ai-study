# 2026-04-19 세션 16 회고 — NEXT.md 큐 소화 + 그래프 시각 개선

## 이번에 한 것

1. **computeGraphSignals vitest 보강** — 함수를 `scripts/lib/graph-signals.mjs`로 분리, 10 케이스 (weakHub 5 + connectivity 3 + edge 2)
2. **승격 자동 스캔 CI** — `promotion-scan.yml` (push + weekly 트리거 → Issue 자동 생성)
3. **추천 다양성 cap** — `generate-lesson.mjs` 카테고리당 max 2건 제한
4. **지식 그래프 Obsidian 스타일 레이아웃** — charge/radial/distanceMax 파라미터 튜닝으로 원형 수렴

## 잘된 것

- NEXT.md 큐 기반으로 High 2건 + Medium 1건을 한 세션에 효율적으로 소화
- computeGraphSignals 분리 → 테스트 가능 + generate-lesson.mjs 코드 깔끔해짐
- 그래프 레이아웃 개선을 유저 요청 즉시 반영, 브라우저 실시간 확인으로 파라미터 수렴

## 아쉬운 것

- **승격 CI 워크플로우 실제 동작 미검증** — yml만 커밋하고 실제 트리거 테스트를 안 함. `workflow_dispatch`로 수동 실행해서 Issue 생성까지 확인했어야 함 (내 판단: "빌드 통과하면 될 것"이라는 추정)
- **그래프 파라미터 3회 반복 튜닝** — charge/radial 관계를 처음부터 계산으로 좁혔으면 2회에 수렴 가능. 감 대신 공식(반발 vs 인력 균형점)을 먼저 추정하는 습관 필요

## 다음에 적용할 것

- CI 워크플로우 추가 시 `gh workflow run` 또는 `act`로 로컬 테스트 후 커밋
- d3-force 파라미터 튜닝 시 노드 수 기반으로 적정 charge 범위 사전 계산 (N개 노드 → charge ≈ -2*N)

## Compound Assets

| 유형 | 파일 | 설명 |
|---|---|---|
| 테스트 | `scripts/__tests__/graph-signals.test.mjs` | computeGraphSignals 10케이스 |
| CI | `.github/workflows/promotion-scan.yml` | 승격 자동 스캔 + Issue |
| 코드 | `scripts/lib/graph-signals.mjs` | 함수 분리 (export) |
| 코드 | `scripts/generate-lesson.mjs` | 다양성 cap |
| UI | `src/components/knowledge-graph.tsx` | Obsidian 스타일 레이아웃 |

## 수치

| 항목 | 값 |
|---|---|
| 커밋 수 | 4 |
| 파일 변경 | 5 |
| 삽입/삭제 | +309 / -43 |
| 테스트 | 33/33 (3 파일) |
| 빌드 | 통과 |

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|---|---|---|
| computeGraphSignals 인라인 함수라 테스트 불가 | lib/로 분리 + export | scripts/lib/graph-signals.mjs |
| 승격 스캔이 수동 실행만 | CI 자동 트리거 + Issue 생성 | .github/workflows/promotion-scan.yml |

## Anti-Rationalization Guard

### 바이어스 점검
- "특이사항 없음" → **아쉬운 점 2건 작성** (CI 미검증 + 파라미터 반복)
- 외부 귀인 → "CI가 복잡해서"가 아니라 "내가 로컬 테스트를 안 함"으로 귀인
- 테스트 없이 추정 → promotion-scan.yml이 실제 GitHub에서 동작할지 미검증 (yml 문법은 통과하지만 jq 파이프, 라벨 존재 등 런타임 이슈 가능)
