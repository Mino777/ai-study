# 2026-04-19 Session 16e — perpetual-engine 분석 + 3대 패턴 이식

## 이번에 한 것

1. `/ingest` 실행: greatsk55/perpetual-engine GitHub 레포 분석
   - gh API + WebFetch + WebSearch 교차 검증 (WebSearch 결과 없음 — 2일 전 생성)
   - 소스코드 6개 파일 직접 분석 (metrics-evaluator, orchestrator, message-queue, consultant-factory, context-manager, phases)
2. `agents/perpetual-engine-portable-patterns.mdx` 엔트리 작성
   - 이식 가능 패턴 3개 + 우리 모델 매핑 테이블 + quiz 3문항
   - 기존 3개 엔트리에 역링크 추가
3. 3대 패턴 즉시 이식
   - `/compound` Phase 1b: MetricsEvaluator KPI 자동 판정
   - `/consult`: ConsultantFactory 에페메럴 전문가 소환
   - `/message` + `messages/`: MessageQueue 파일 기반 비동기 메시징
   - `/projects-sync` 메시지 큐 읽기 연동

## 잘된 것

- **분석→이식 원스톱**: 엔트리 작성(학습)과 실제 이식(실행)을 한 세션에서 완료. 보통 분석만 하고 이식은 "다음에"로 미루는 패턴을 깼다
- **소스코드 직접 분석**: README만 읽지 않고 핵심 .ts 파일을 직접 읽어 설계 의도까지 파악. MetricsEvaluator의 "중간 체크포인트 관대함" 같은 세부 설계를 캐치
- **JSX 파싱 에러 즉시 대응**: `<30%`가 JSX로 파싱되는 문제를 빌드에서 바로 잡음

## 아쉬운 것

- **서브에이전트 파일 저장 실패**: 분석 서브에이전트가 /tmp에 파일을 저장했다고 했지만 실제로는 없었다. 서브에이전트 결과물을 신뢰하지 말고 직접 확인하는 습관이 아직 부족 — 결국 직접 gh api로 다시 가져왔으므로 시간 낭비
- **교차 검증 소스 부족**: 레포가 2일 전 생성이라 WebSearch 결과 0건. 독립 소스 교차가 불가능했고, README + 소스코드라는 "같은 저자" 2개 소스에 의존. /ingest의 "최소 2개 독립 소스" 원칙을 완전히 충족하지 못함

## KPI 판정

**KPI 미정의** — NEXT.md에 이번 스프린트 KPI 섹션이 없었음. 다음 NEXT.md에 KPI 2~3개 정의 권장.

## 다음에 적용할 것

- `/consult` 첫 실전 사용: 다음 세션에서 적절한 자문 상황이 오면 실행해서 패턴 검증
- `/message` 첫 실전 사용: 워커 세션 시작 시 허브에서 보낸 directive 확인 흐름 테스트
- NEXT.md에 KPI 2~3개 정의: MetricsEvaluator가 작동하도록 baseline + target 설정
- 서브에이전트 결과물은 반드시 파일 존재 확인 후 사용

## Compound Assets

- **슬래시 커맨드 2개**: `/consult`, `/message` — 다음 세션부터 즉시 사용 가능
- **MetricsEvaluator Phase**: `/compound`에 KPI 자동 판정 단계 영구 탑재
- **perpetual-engine 엔트리**: 외부 프레임워크 분석 → 이식 패턴 추출의 모범 사례

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| /compound에 KPI 판정 부재 | MetricsEvaluator Phase 1b 추가 | .claude/commands/compound.md |
| 워커에 비동기 지시 불가 | /message 메시지 큐 추가 | .claude/commands/message.md |
| 도메인 전문가 자문 부재 | /consult 에페메럴 전문가 추가 | .claude/commands/consult.md |

## 수치

| 항목 | 값 |
|---|---|
| 커밋 수 | 2 |
| 엔트리 생성 | 1 |
| 슬래시 커맨드 신규 | 2 |
| 역링크 추가 | 3 |
| 분석 소스 파일 | 6 |
