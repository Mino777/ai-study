# 2026-04-20 회고 — 세션 17: 위키 AI 에이전트 실용성 강화

## 이번에 한 것

1. **프로젝트 씽크** — moneyflow/tarosaju/aidy×4 전체 pull + 상태 확인. aidy가 4개 레포임을 재확인.
2. **OpenMythos 엔트리 재작성** — Gemini 자동생성 슬롭을 하네스/컴파운드 비교 분석으로 전면 재작성
3. **bash-watcher 패턴 추출** — aidy-architect s27 실제 코드에서 추출. LLM 폴링 토큰 0 달성 패턴 박제
4. **위키 AI 실용성 자기 비판** — "위키가 사람용 서사 중심, AI가 즉시 행동 연결 어렵다" 구조적 문제 식별
5. **대규모 개선 실행** — 스키마 확장, 144개 백필, AI Directive 추가, SummaryCard UI, 검색 벤치마크 자동화

## 잘된 것

- **자기 비판 → 즉각 실행**: 문제 인식 후 5개 개선을 한 세션에 완주. 글과 행동 일치
- **aidy 실제 코드에서 패턴 추출**: aspirational 글 대신 s6→s26→s27 커밋 히스토리를 직접 읽고 검증된 진화 과정 박제
- **백필 스크립트 설계**: 144개 파일 수동 편집 대신 스크립트 작성 → dry-run 검증 후 apply. 재사용 가능한 도구로 남김
- **projects-sync 범위 확장 + 메모리 저장**: aidy 빠진 것을 사용자가 지적하자 즉시 메모리에 박제

## 아쉬운 것

- **backfill applicable_to 추론 단순화**: 모든 엔트리를 `["any"]` 기본으로 처리하고 aidy 키워드 있는 것만 `["aidy", "any"]` 로 했는데, 실제로는 moneyflow/tarosaju 특화 패턴도 있다. 파일명/내용 분석이 충분히 정밀하지 않았다. **내 판단으로 추정 정밀도를 과대평가함.**
- **SummaryCard 검증 미흡**: UI 변경 후 실제 브라우저에서 배지 렌더링을 확인하지 않았다. 빌드 통과만 확인. 시각적 검증 생략.
- **weekly-search-benchmark.yml 실행 검증 없음**: shadow-benchmark.mjs의 출력 포맷이 grep 패턴과 맞는지 로컬에서 테스트하지 않고 워크플로우를 작성했다. 첫 월요일에 파싱 실패할 가능성 있다.

## 다음에 적용할 것

- **워크플로우 스크립트 작성 시 로컬 dry-run 먼저**: `act` 또는 실제 스크립트 직접 실행으로 파싱 패턴 검증 후 커밋
- **applicable_to 2차 정밀화**: 카테고리별로 기본값 다르게 — journal 시리즈는 프로젝트 특화, pattern 엔트리는 any

## KPI 판정

KPI 미정의 (이전 NEXT.md에 KPI 섹션 없음) — 다음 NEXT.md에 KPI 정의 권장.

실측:
- 엔트리 수: 144 → 146 (목표 미정의)
- AI Directive 커버리지 (harness): ~85% → 100%
- 자동화 워크플로우: 5 → 6

## 프로세스 개선

| 재료 | 개선 | 파일 |
|---|---|---|
| projects-sync에서 aidy 반복 누락 | 메모리에 박제 + 6개 레포 씽크 루틴 확립 | memory/feedback_projects_sync_includes_aidy.md |
| AI 자동생성 엔트리 품질 미검증 | SummaryCard 🤖 AI 생성 배지 + last_verified 표시 | summary-card.tsx |
| JIT 검색 품질 주관적 판단 의존 | weekly shadow-benchmark 자동화 | weekly-search-benchmark.yml |

## Compound Assets

- `scripts/backfill-frontmatter.mjs` — 재사용 가능한 MDX frontmatter 백필 도구
- `bash-watcher-zero-token-polling.mdx` — aidy 실전 검증 패턴
- `openmythos-architecture-iterative-agent-performance.mdx` — 3개 피드백 루프 비교 프레임워크
