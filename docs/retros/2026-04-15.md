# 2026-04-15 회고 — 보안 스프린트 + n8n 리서치

## 이번에 한 것

1. **보안 전수 조사 + 3 Sprint 실행** — ai-study 허브의 Critical 4건 + High 3건 취약점 수정
2. **n8n 도입 리서치** — 7 워크플로 설계 + 대안 비교 + 4 Phase 로드맵 문서화
3. **Wiki 엔트리 박제** — `security-hardening-checklist.mdx` (7개 보안 패턴)
4. **NEXT.md 싱크** — 82→83 엔트리, 보안 스프린트 완료 반영, 큐 재정렬

## 잘된 것

- **허브-워커 역전 발견**: moneyflow/tarosaju가 이미 CSP, Rate Limiting, Timing-Safe를 갖추고 있었는데 허브(ai-study)가 가장 취약했음. 이 발견 자체가 보안 감사의 가치
- **Edge Runtime 호환 패턴**: `crypto.timingSafeEqual` → Web Crypto HMAC XOR 비교로 전환. 재사용 가능한 패턴 확보
- **Sprint 분할 효과**: Critical(즉시) → High(이번 주) → Medium(다음 주)로 나눠서 Sprint 1+2를 한 세션에 모두 완료. 과부하 없이 핵심 수정
- **npm audit 0 vulnerabilities**: Next.js CVE 패치 후 깨끗한 상태 달성

## 아쉬운 것

- **auth.ts 첫 시도 실패**: `requireEnv`를 모듈 top-level에 놓아서 빌드 실패 → Edge Runtime 제약을 미리 고려했으면 1회 시도로 성공했을 것. Solution 문서로 박제
- **moneyflow/tarosaju npm audit CI는 미이식**: ai-study만 적용. 워커 프로젝트는 다음 세션 큐에 남김

## 다음에 적용할 것

- Edge Middleware가 import하는 파일은 Node.js built-in 사용 금지 (solution 박제됨)
- 환경변수 검증은 항상 lazy function으로 (top-level const 금지)
- 보안 감사는 허브 먼저 → 워커 이식 순서 (허브가 가장 노출도 높음)

## Compound Assets

| 유형 | 이름 | 위치 |
|------|------|------|
| Wiki 엔트리 | 보안 강화 체크리스트 | `content/harness-engineering/security-hardening-checklist.mdx` |
| Solution | Edge Runtime Timing-Safe | `docs/solutions/workflow/2026-04-15-edge-runtime-timing-safe-comparison.md` |
| 리서치 문서 | n8n 도입 분석 | `docs/n8n-adoption-research.md` |
| 인프라 | Rate Limiter | `src/lib/rate-limit.ts` |
| 인프라 | Security Headers | `next.config.ts` |
| CI | npm audit gate | `.github/workflows/ci.yml` |

## 수치

| 항목 | 값 |
|------|-----|
| 보안 취약점 수정 | 7건 (Critical 4 + High 3) |
| npm audit 결과 | 1 high → 0 |
| 파일 변경 | 14 files, +720 / -78 |
| 위키 엔트리 | 82 → 83 |
| 신규 문서 | 3 (wiki + solution + n8n research) |
