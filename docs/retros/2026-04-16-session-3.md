# 2026-04-16 세션 3 회고 — Flow Map 완주(6/N) + Context Scaling 방향성 박제 + NEXT.md 60KB→7.6KB

## 이번에 한 것

1. **Context Scaling 3-레이어 아키텍처** 박제 (`context-engineering/context-scaling-3-layer-architecture.mdx`, 650줄) — Prompt Caching · Pruning/Compression · JIT Retrieval 3 레이어로 "지식 = 토큰" 선형 트레이드오프 끊기. 4 조건 게이트 기반 도입 순서. 4 프로덕트(ai-study/moneyflow/tarosaju/aidy) 매핑
2. **핸드오프 문서 함정** 특별 섹션 추가 — "단순 핸드오프는 삭제가 정답". ai-study NEXT.md 60KB 실제 사례로 증거 제시. 대체 구조(정보 유형별 위치 매트릭스) 포함
3. **Flow Map 4·6·7편 연작 작성**
   - 4편: `architect-flow-map-via-aidy-architect` (382줄) — harness-engineering
   - 6편: `api-contract-as-3-client-source-of-truth` (393줄) — context-engineering
   - 7편: `test-strategy-3-client-via-aidy` (429줄) — evaluation
4. **Part 5 대체** (`aidy-3-client-deployment-design-roadmap.mdx`) — 실 배포 미구축이라 Flow Map 장르 대신 "설계 로드맵" 장르로 대체. Neon/Fly/Sentry 선택 근거 + Stage 0~8 도입 순서 + 4 조건 게이트 적용
5. **Dangling connections 4건 해소** — llm-as-judge-overview → pattern · tokenomics-catalog → claude-code-token-levers-catalog · nextjs-app-router-patterns → frontend-ai-patterns · ci-cd-patterns 제거
6. **NEXT.md 60KB → 7.6KB** (-87%) — append-only 운영 규칙 폐기 · 세션 경계 교체로 전환. **핸드오프 섹션에서 스스로 제안한 액션을 같은 세션에서 실행 (dogfood)**
7. **시리즈 기획 문서 갱신** — 상태 3/N → 6/N, Part 5 deferred 처리

## 잘된 것

- **Dogfood 원칙 완결**: 핸드오프 함정 섹션에서 "60KB NEXT.md 는 정리 대상" 이라 썼는데 같은 세션에서 실제로 정리. 글과 행동의 일치가 article 의 신뢰도를 높임
- **Part 5 장르 전환 판단**: "aspirational flow map 이 되면 memory rule 위반" 이라는 리스크를 미리 인지하고 Flow Map → 설계 로드맵 장르 전환. 시리즈 일관성 유지하면서 실재성 존중
- **3 Flow Map 연작을 한 세션에**: 4편 · 6편 · 7편 각 380~430줄. 소스 레포(aidy-architect)를 깊이 읽은 덕분에 각 편이 구체 참조 · 실제 파일 경로 · 실측 사고(Session 4 iOS 테스트 미실행) 인용 가능
- **Context Scaling 글의 "다음 연구 과제 7" 이 실행 가능한 수준**: 청크 크기 · 라우터 정밀도 · Reranker 경제성 등이 POC 로 바로 연결되는 구체 질문
- **Dangling 4건 접근**: 무조건 새 엔트리 만들지 않고 "기존 파일명 근접 매칭 3 + 제거 1" 로 저비용 해소. "대체 가능한 곳은 redirect · 아니면 제거" 판단 기준

## 아쉬운 것

- **Mermaid 4번째 재발**: cylinder 노드 `[("...")]` + 내부 괄호 = validator 오탐. 이번 세션에 `<br/>` 패턴(3번째)도 있었고 바로 연속으로 cylinder 패턴(4번째) 발생. `mermaid-fix.mjs` 확장을 또 백로그로 넘김 — 이 패턴은 다음 Flow Map 작성 시 반드시 재발할 것
- **JSX brace 함정 4회 반복**: `{backlog,in-progress,done}` · `{slug,category,tags,date,confidence}` · `{slug,date,confidence,tags}` · `{worker}` — 각 엔트리 작성마다 같은 함정. Pre-write validation 또는 template 에 주의 섹션 넣는 게 낫겠음
- **`<1분 · <3분` 패턴 재발**: 기존 솔루션 `2026-04-12-jsx-parsing-traps-curly-angle.md` 에 박제됐는데도 또 썼음. 메모리 주입이 약함 — grep-based pre-commit hook 필요
- **NEXT.md 정리의 범위 제한**: 완료 큐 제거는 했지만 CLAUDE.md 의 "세션 시작 4 파일 로드 필수" 규약은 건드리지 않음. 이건 사용자 결정 영역이라 보류 — 다음 세션에서 사용자와 재검토 필요

## 다음에 적용할 것

1. **mermaid-fix.mjs 확장** — `<br/>` · 특수문자 · cylinder `[("...")]` + 내부 괄호 3 패턴 auto-fix 추가. idempotency 테스트 선행 (`scripts/__tests__/mermaid-fix.test.mjs`)
2. **MDX pre-commit 가드** — `grep -nE '{[a-z0-9_]|<[0-9]|\[\("[^"]*\('` 로 JSX brace · angle · cylinder-parens 3 패턴 사전 탐지. 매칭 시 커밋 차단
3. **CLAUDE.md 4 파일 로드 규약 재검토** — NEXT.md 의 역할 축소 후 정말 "필수" 인지 사용자 결정 받기
4. **Context Scaling Layer 3 POC 시작** — ai-study 에서 Chroma 로컬. 섀도우 모드 1주. 이번 세션 글의 "다음 연구 과제" 1~2번 착수
5. **Series plan 문서 유지 규약** — 새 편 완료 시 기획 문서의 "출간 완료" 표 자동 갱신 (수동 빠짐 방지)

## Compound Assets

- **솔루션 신규 1건** (`mdx/2026-04-16-mermaid-cylinder-nested-parens.md`) — 4번째 재발 mermaid 패턴. 다른 Flow Map 작성자에게 즉시 이식 가능
- **설계 로드맵 장르 확립** — "실 구현 전 선제적 박제" 형태. 다른 사람 프로젝트의 "어떻게 시작할지" 설계 단계 글에 템플릿 재사용 가능. confidence 2 로 운영 → 실측 후 3~4 격상 규약
- **핸드오프 함정 섹션** (`context-scaling-3-layer-architecture` 내) — 업계적으로도 드물게 정리된 관점. 외부 공유 시 강한 차별점
- **NEXT.md 운영 규칙 전환** — "세션 경계에서 교체" 패턴을 다른 프로젝트에 이식 가능 (aidy HANDOFF.md 는 이미 이 패턴)
- **Dogfood 완결 사례** — "글에서 제안한 것을 같은 세션에서 실행" 이 신뢰도 · 재사용성 양쪽 강화. 향후 설계 글 작성 시 이 패턴 우선 고려

## 수치

| 항목 | 값 |
|------|-----|
| 신규 엔트리 | 5건 (context-scaling · architect-flow-map · api-contract · test-strategy · deployment-roadmap) |
| 엔트리 수정 | 4건 (dangling 3 redirect + 1 제거) |
| 문서 정리 | NEXT.md 60KB → 7.6KB (-87%) |
| 신규 솔루션 | 1건 (mermaid cylinder parens) |
| 커밋 | 5건 (content · handoff section · 6편 · 7편 · sprint) |
| 변경 라인 (순증) | 약 +2400 / -1060 |
| 빌드 | 105 static paths |
| 총 엔트리 | 104 → **105** |
| Mermaid 재발 패턴 | 4번째 (cylinder + parens) — solution 박제로 5번째 방지 시도 |
