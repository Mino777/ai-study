---
date: 2026-04-09
scope: 스프린트 데이 — 콘텐츠 3개 + UX 5개 + Admin 에디터 + SEO/접근성
---

# 스프린트 회고 — 2026-04-09

## 완료한 작업

### 콘텐츠
- **Context Engineering** MDX 엔트리 작성 (frontmatter + 다이어그램 포함)
- **Harness Engineering** MDX 엔트리 작성 (CLAUDE.md 시스템 아키텍처 기반)
- **Evaluation** MDX 엔트리 작성 (평가 메트릭 + 예제)

### UX 개선 (5개)
- Coming Soon 플레이스홀더 (빈 카테고리용)
- 지식 그래프 회색 노드 (미작성 항목 시각화)
- Related Entry 카드 (엔트리 하단 연관 학습 추천)
- Learning Heatmap (학습 스트릭 + 일별 활동 시각화)
- OG 이미지 생성 (카테고리별 동적 소셜 미리보기)

### Admin 에디터
- GitHub API 기반 전체 CRUD (MDX 파일 관리)
- MDX 에디터 UI (frontmatter 폼 + 콘텐츠 에디터)
- 인증 시스템 (GitHub OAuth 또는 토큰 기반)

### SEO & 접근성
- JSON-LD 스키마 (Article, BreadcrumbList, FAQPage 타입)
- Canonical URL 태그 자동 추가
- 메타데이터 완성 (title, description, keywords per entry)
- ARIA 레이블 전체 적용 (네비게이션, 그래프, 목차, 모달)
- 키보드 네비게이션 강화 (Tab, Enter, Escape 지원)
- 색상 대비 검증 (WCAG AA 통과)

### Compound Engineering
- `/compound` CLI 명령어 구현
- Git 훅 (pre-commit: 빌드 게이트, post-push: 배포 상기)
- moneyflow COMPOUND-GUIDE.md 패턴 이식

### 인프라 & CI/CD
- 일일 학습 주제 추천 cron 수정 (새 레포 스케줄 지연 해결, offset 적용)
- GitHub Actions 빌드 최적화

## 잘된 점

1. **콘텐츠-코드 순환 가속화**
   - GEMINI_API_KEY 없이도 수동 작성으로 3개 엔트리 완성
   - MDX 에디터를 통한 마크다운 → 지식 그래프 통합이 워크플로우 단순화

2. **Compound Engineering 확립**
   - `/compound` 명령어 + Git 훅으로 반복 작업 자동화
   - moneyflow에서 검증된 패턴을 빠르게 이식 (코드 재사용성 ↑)

3. **UX-SEO 동시 진행**
   - 5개 UI 개선과 SEO 구현을 병렬로 처리
   - Related Entry 카드 + Learning Heatmap이 사용자 몰입도 향상

4. **카테고리 컨솔리데이션**
   - CATEGORY 상수를 10개 파일에서 `schema.ts`로 통합 (-168줄)
   - DRY 원칙으로 유지보수 부담 ↓

5. **접근성을 처음부터 구축**
   - ARIA 레이블, 키보드 네비게이션, 색상 대비를 엔트리 단위에서 체계화
   - 다음 엔트리 추가 시 접근성 비용 ↓

## 개선할 점

1. **콘텐츠 생성 자동화 회귀**
   - GEMINI_API_KEY 로컬 환경 미설정 → 다시 자동화 파이프라인 활성화 필요
   - 향후 GitHub Actions 크론 재확인

2. **Admin 에디터 권한 관리**
   - 현재 GitHub OAuth 기반이지만, 팀 협업 시 역할별 권한 설계 필요
   - 예: 리뷰어, 편집자, 뷰어 구분

3. **그래프 성능 최적화**
   - 노드 수 증가 시 렌더링 지연 가능 → linkDistance, charge 튜닝 필요
   - 대규모 데이터셋은 Virtual Scroll 고려

4. **지표 추적 미흡**
   - Learning Heatmap은 UI만 구현, 백엔드 데이터 수집 (analytics) 미진행
   - 사용자 학습 패턴 기반 추천 알고리즘 아직 미구현

5. **문서화 격차**
   - Admin 에디터 사용법, `/compound` CLI 옵션을 README에 정리하지 않음
   - 다음 배포 전 docs 업데이트 필수

## 복리 자산 (다음 작업을 빠르게 만드는 것)

1. **`/compound` CLI**
   - 새 엔트리/카테고리 추가 시 폴더 구조 + 메타데이터 자동 생성
   - Git 훅으로 커밋 전 빌드 검증 자동화
   - *다음 엔트리 추가 시간: ~30분 → ~5분*

2. **Admin 에디터 CRUD 기반**
   - GitHub API 추상화 완료 (파일 생성/수정/삭제)
   - MDX 파서 + frontmatter 폼 재사용 가능
   - *웹 UI를 통한 편집 가능 → 팀 협업 개시*

3. **OG 이미지 생성기**
   - 카테고리별 동적 소셜 미리보기 자동화
   - 새 엔트리 추가 시 자동 생성 가능
   - *SNS 공유 시 engagement ↑*

4. **ARIA + 키보드 네비 템플릿**
   - SearchDialog, Sidebar, MobileNav 패턴 정립
   - 새로운 모달/드롭다운 구현 시 복사-붙여넣기로 접근성 확보
   - *QA 시 접근성 이슈 감소*

5. **JSON-LD 스키마 제너레이터**
   - Entry, Category, Graph 타입 자동 생성
   - 이제 새 엔트리는 자동 JSON-LD 포함
   - *SEO 점수 유지, Structured Data 완성도 ↑*

6. **CATEGORY 스키마 통합**
   - `src/lib/schema.ts`에서 단일 진실 공급원 확립
   - 타입 안정성 + 마이그레이션 안전
   - *코드 변경 시 오류 가능성 ↓*

## 수치

| 항목 | 값 |
|------|-----|
| **파일 변경** | 42개 |
| **새 파일** | ~25개 |
| **코드 추가** | +2,812줄 |
| **코드 삭제** | -183줄 |
| **엔트리 수** | 11 → 14 (+3) |
| **빈 카테고리** | 5 → 2 (-3) |
| **커밋** | 10개 |
| **Git 훅 추가** | 2개 (pre-commit, post-push) |
| **ARIA 레이블 추가** | ~30개 |
| **JSON-LD 타입** | 3개 (Article, BreadcrumbList, FAQPage) |

---

**다음 스프린트 포커스:**
- GEMINI_API_KEY 자동화 파이프라인 복구
- Admin 에디터 권한 시스템 설계
- Learning Heatmap 백엔드 데이터 수집 (analytics)
- 문서 업데이트 (Admin 에디터, `/compound` CLI)
