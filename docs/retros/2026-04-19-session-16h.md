# 2026-04-19 세션 16h — Hermes 불필요 판정 + Flow Map s27 + Mermaid 근본 해결

## 이번에 한 것

1. generated_by: gemini 소급 적용 (12건)
2. Compiled Truth 일괄 적용 (4 카테고리)
3. GBrain+OpenClaw+Hermes 3스택 검토 → 현재 불필요 판정, 엔트리 업데이트
4. /projects-sync로 aidy 4레포 스캔 → Flow Map 4편 s27 업데이트 (v2.3~v2.6)
5. Mermaid 렌더링 에러 3중 근본 원인 해소:
   - `<br/>` auto-fix 승격
   - 콜론 unquoted auto-fix 추가
   - subgraph/node ID 충돌 감지 error 추가

## KPI 판정

| 지표 | baseline | target | actual | 달성률 | verdict |
|------|----------|--------|--------|--------|---------|
| 총 엔트리 수 | 143 | 148 | 144 | 20% | stagnant |
| Hermes 로컬 설치 | 0 | 1 | 1 | 100% | achieved |
| /message poll 실전 | 0 | 1 | 0 | 0% | N/A (Hermes 불필요 판정) |

**종합 verdict**: 엔트리 증가는 미달이나, Hermes 검토 완료 + Mermaid 근본 해결이 핵심 성과. 방향 전환(Hermes 불필요)이 오히려 올바른 판단.

## 잘된 것

- **Hermes 불필요 판정이 빨랐음** — 검토 1세션 만에 "Claude Code Max + Gemini로 충분"이라는 결론. 매몰 비용 함정에 빠지지 않음
- **Mermaid 3중 원인 순차 해소** — `<br/>` 수정 후에도 에러 → 콜론 → ID 충돌까지 끝까지 추적
- **mermaid-fix.mjs auto-fix 승격 결정** — warning-only로 5번 재발한 패턴을 드디어 auto-fix로 격상

## 아쉬운 것

- **Mermaid 에러 3중 원인을 한 번에 못 잡음** — `<br/>` 수정 → 배포 → 아직 에러 → 콜론 수정 → 배포 → 아직 에러 → ID 충돌 발견. 처음부터 Mermaid를 로컬에서 렌더 테스트했으면 3단계를 1단계로 줄일 수 있었음. 증상별로 하나씩 고친 게 아니라 **한꺼번에 모든 가능 원인을 스캔하는 접근**이 필요했음
- **나머지 63건 subgraph/node ID 충돌 미수정** — flow map 2편만 고치고 나머지는 방치. 시간 제약이 아니라 범위를 좁게 잡은 판단 실수

## 다음에 적용할 것

- Mermaid 에러 수정 시: 개별 수정 후 배포 확인이 아니라, **로컬에서 실제 렌더링 확인 후 커밋**
- subgraph/node ID 충돌 63건 일괄 정리 (다음 세션)
- mermaid-fix.mjs의 error 감지가 prebuild에서 실제로 빌드를 차단하는지 확인 필요

## Compound Assets

| 자산 | 파일 | 다음 세션 활용 |
|------|------|--------------|
| Compiled Truth 4건 | docs/solutions/\*/_compiled-truth.md | 솔루션 빠른 파악 |
| mermaid-fix auto-fix 3규칙 | scripts/lib/mermaid-fix.mjs | `<br/>`, 콜론, 괄호 자동 수정 |
| mermaid-fix ID 충돌 감지 | scripts/lib/mermaid-fix.mjs | 빌드 시 에러 리포트 |
| Flow Map s27 | content/\*\*/\*flow-map\*.mdx | 학습 가이드 최신화 |

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| Mermaid `<br/>` 5번 재발 | warning → auto-fix 승격 | scripts/lib/mermaid-fix.mjs |
| Mermaid 콜론 unquoted 재발 | auto-fix 3 추가 | scripts/lib/mermaid-fix.mjs |
| subgraph/node ID 충돌 미감지 | error 감지 추가 | scripts/lib/mermaid-fix.mjs |
| 테스트 16→34 확대 | 3개 auto-fix + ID 충돌 테스트 | scripts/__tests__/validate-content.test.mjs |

## 수치

- 커밋: 6건
- 파일 변경: 23 files, +447 -251
- 테스트: 34/34 PASS
- Mermaid `<br/>` 제거: 126건
- Mermaid 콜론 수정: 18건
