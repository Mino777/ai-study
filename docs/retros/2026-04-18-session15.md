# 2026-04-18 Session 15 — 크로스 세션 리뷰 + Graph-Lesson 연동

## 이번에 한 것

1. **크로스 세션 리뷰 (세션 14 검증)**: 5단 프로토콜 실행, 6 함정 검증, URL 10건 WebFetch 재검증
2. **딥리서치 엔트리 confidence 교정**: 3건 confidence:3→2 하향 (메모리 룰 `feedback_research_entry_confidence` 적용)
3. **멀티에이전트 엔트리 현실 반영**: Hub-Worker 섹션의 "진화 방향" 테이블을 "달성 vs 실제 다음 단계"로 교체. 이미 병렬 세션 운영 중인 현실 반영
4. **graph-query → generate-lesson 연동**: `computeGraphSignals()` 함수 추가, 약한허브 카테고리 +1.5점, 고연결+저confidence +1.0점 부스트

## 잘된 것

- 크로스 세션 리뷰에서 confidence 과대 문제를 구조적으로 잡아냄 (이전 세션 자기 점검과 일치)
- 유저가 "이미 병렬 에이전트로 하고 있지 않나?"라고 지적 → 엔트리의 현실 괴리를 즉시 수정
- graph-lesson 연동이 M 사이즈 예상대로 깔끔하게 완료 (46줄 추가, 기존 로직 비파괴)

## 아쉬운 것

1. **`computeGraphSignals` 테스트 미작성**: 순수 함수인데 vitest 케이스 0건. 그래프 구조가 바뀌면 회귀 감지 불가
2. **graph 부스트 밸런싱 미검증**: +1.5 / +1.0 점수가 기존 스코어(category priority 2.0x, staleness +0.5)와 적절히 균형 잡히는지 실측 데이터 없이 감으로 설정. 추천 결과가 context-engineering에 편중될 가능성

## 다음에 적용할 것

- `computeGraphSignals` vitest 케이스 추가 (weakHubCategories 검출 + categoryConnectivity 계산)
- 추천 결과 다양성 확인: 같은 카테고리 3건 연속 추천 방지 로직 검토
- graph 부스트 점수를 2-3 세션 관찰 후 조정

## Compound Assets

| 유형 | 파일 | 내용 |
|------|------|------|
| 엔트리 수정 | multi-agent-orchestration-patterns-2026.mdx | Hub-Worker 현실 반영 |
| 스크립트 | generate-lesson.mjs | graph signal 연동 (+46줄) |

## 프로세스 개선 (이번 스프린트)

| 재료 | 결론 | 파일 |
|------|------|------|
| 리서치 엔트리 confidence 과대 | 이미 메모리 룰 존재 → 크로스 세션 리뷰에서 집행됨 | (기존 메모리) |
| 엔트리 "진화 방향"이 현실과 괴리 | 리서치 기반 엔트리의 "현재 상태" 기술은 실제 운영과 교차 검증 필요 | (엔트리 직접 수정) |

## 수치

| 항목 | 값 |
|------|---|
| 커밋 수 | 3 |
| 엔트리 수정 | 4 (confidence 3건 + hub-worker 1건) |
| 스크립트 변경 | 1 (generate-lesson.mjs +46줄) |
| URL 검증 | 10건 중 8 통과, 1 봇차단(403), 1 레이트리밋(429) |
| 빌드 | PASS |
| 테스트 | 23/23 |
