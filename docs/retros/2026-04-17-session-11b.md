# 세션 11b 회고 — JIT 검색 4프로젝트 이식 + 대시보드 버그 수정

- **일시**: 2026-04-17
- **커밋**: ai-study 1건 (대시보드 수정) + 외부 3건 (moneyflow/tarosaju/aidy-architect JIT 이식)
- **엔트리**: 129 (변동 없음)

## 이번에 한 것

1. **대시보드 JIT 통계 버그 수정** — solutions 경로가 제목 없이 표시되는 문제
2. **JIT 검색 3프로젝트 동시 이식** — moneyflow(354청크) + tarosaju(227) + aidy-architect(308)
3. **시각화 페이지 생성** — Before/After 비교 + 프로젝트별 토큰 절감 효과 + 아키텍처 다이어그램

## 잘된 것

- 3 에이전트 병렬 실행으로 3 프로젝트 동시 이식 — 각각 ~5분 내 완료
- 유저가 "리스크 낮으니까 전부 이식하자" 판단 → 기존 "3세션 검증 후 이식" 전략 오버라이드 → 결과적으로 옳은 판단

## 아쉬운 것

- **대시보드 버그를 유저가 스크린샷으로 발견** — 내가 JIT 히트 카운트 구현 시점(세션 10)에서 solutions 경로가 hits에 포함되는 걸 예측하지 못한 설계 누락. `entries` 배열과 `hits` 키의 도메인 불일치를 사전에 확인했어야 함.
- **이식 스크립트 템플릿화 안 함** — 3 프로젝트에 거의 동일한 코드를 각각 복사. 공유 패키지나 스크립트 템플릿을 만들면 4번째 프로젝트부터 더 빠를 텐데, 현재 4개면 충분하므로 과잉 추상화일 수 있음.

## 다음에 적용할 것

- 외부 데이터(JSON)를 UI에 바인딩할 때, 데이터 키의 도메인과 UI 엔티티의 도메인이 일치하는지 사전 검증
- JIT 이식 시 embed-content 실행 후 검색 테스트 1건 필수 (3 프로젝트 모두 이미 수행)

## Compound Assets

| 유형 | 파일 |
|------|------|
| Solution | docs/solutions/next-patterns/2026-04-17-dashboard-jit-stats-solutions-filter.md |
| 대시보드 수정 | src/app/dashboard/page.tsx (hitEntries 필터링) |
| JIT 이식 | moneyflow/tarosaju/aidy-architect 각 scripts/ + CLAUDE.md |

## Phase 3b: Anti-Rationalization Guard

### 바이어스 패턴 1: "특이사항 없음"
- 아쉬운 점 2건 기록 완료.

### 바이어스 패턴 2: 외부 귀인 리프레이밍
- "유저가 스크린샷으로 발견" → 내가 구현 시점에 도메인 불일치를 예측 못한 설계 누락으로 귀인 완료.

### 바이어스 패턴 3: 자기 점검
- 건너뛴 것? → 이식 후 각 프로젝트에서 shadow-benchmark 수준의 적중률 측정을 안 함 — 하지만 아직 쿼리가 없으므로 시기상조. OK.
- 에러/경고 무시? → 없음.
- 테스트 없이 추정? → 3 프로젝트 모두 실제 검색 테스트 수행. OK.
- 스코프 축감? → aidy 워커(server/ios/android) 미이식은 유저 동의 하에 결정. OK.

### 2차 방어선
이 세션 커밋 4건 이상 → `/cross-session-review` 추천. 다만 외부 프로젝트 커밋이 대부분이라 ai-study 단독 리뷰로 충분.

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| solutions 경로 대시보드 노출 | hitEntries에 entries 매칭 필터 추가 | dashboard/page.tsx |
| JIT 이식 3회 반복 | 에이전트 병렬 실행 패턴 검증 (향후 재사용) | — |
