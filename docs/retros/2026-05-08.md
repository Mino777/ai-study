# 회고 — 2026-05-08 전체 엔트리 점검 스프린트

## 이번에 한 것

- **21개 draft 엔트리 → complete**: production에서 404였던 엔트리 전부 공개 전환
- **3개 in-progress → complete**: tool-use, rag-overview, vector-databases
- **309개 역링크 일괄 추가**: fix-one-way-connections.mjs --apply로 106개 파일 수정
- **의사코드 3건 정리**:
  - workflow-scaffolding: TypeScript 의사코드 100줄 → 실전 Compound Engineering 사례로 교체
  - design-md: TypeScript 의사코드 → CLAUDE.md 선언적 패턴으로 교체
  - ios-claude-code-mcp: 하드코딩 API → 실제 Anthropic Messages API 호출로 교체
- **ios-ai-developer-growth-direction-2026 404 수정**: status: draft → complete
- **전체 204개 엔트리 품질 스캔**: 13개 카테고리 전수 검사

## 잘된 것

- 병렬 에이전트 스캔으로 204개 엔트리를 3그룹으로 나눠 동시 점검 → 빠른 이슈 파악
- fix-one-way-connections.mjs 스크립트가 이미 있어서 역링크 309개를 1분 만에 해결
- 빌드 성공 확인 후 푸시 → 배포 정합성 보장

## 아쉬운 것

- **draft 21개를 이전 세션에서 놓친 것**: 내가 엔트리 생성 시 status: draft로 남긴 것을 검증하지 않았다. production에서 404가 나는 것을 사용자가 직접 발견하기 전까지 모르고 있었다. → "status 필드 검증"을 빌드 시 자동 경고하는 게 나았을 것
- **역링크 309개 누적**: 엔트리 추가 때마다 양방향 연결을 확인했어야 하는데, 한꺼번에 쌓였다. → 엔트리 생성 파이프라인에 역링크 체크를 넣어야 함
- **의사코드 정리 판단**: workflow-scaffolding의 TypeScript 코드가 "의미없다"는 판단을 사용자가 먼저 해줬다. 내가 자체 점검에서 먼저 잡았어야 했다

## 다음에 적용할 것

1. 엔트리 생성 시 `status: complete` 기본값 강제 또는 draft 경고 출력
2. 엔트리 생성 후 fix-one-way-connections.mjs 자동 실행 (또는 빌드 시 경고)
3. AI 생성 엔트리의 코드 블록에 "의사 코드" / "// 실제로는" 패턴이 있으면 자동 경고

## Compound Assets

| 자산 | 파일 | 재사용 가능성 |
|------|------|---------------|
| draft→complete 일괄 전환 패턴 | sed + grep 원라이너 | 다음 점검 때 재사용 |
| 병렬 품질 스캔 패턴 | 3개 에이전트 동시 스캔 | 정기 QA에 재사용 |
| 역링크 일괄 추가 | fix-one-way-connections.mjs | 이미 스크립트화됨 |

## 수치

- 변경 파일: 120개
- 코드 변경: +2,965 / -1,650
- draft → complete: 21개
- in-progress → complete: 3개
- 역링크 추가: 309개 (106파일)
- 의사코드 정리: 3건
- 최종 엔트리 수: 204개
- 그래프: 207 nodes, 1,509 edges

## KPI 판정

KPI 미정의 — 다음 NEXT.md에 KPI 2~3개 정의 권장.
제안: 엔트리 수(204 baseline), 그래프 edges(1509 baseline), draft 잔여(0 baseline → 0 유지)

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| draft 21개 production 404 | 향후: 빌드 시 draft 엔트리 경고 출력 검토 | generate-content-manifest.mjs |
| 역링크 309개 누적 | 향후: 엔트리 생성 파이프라인에 역링크 체크 통합 검토 | fix-one-way-connections.mjs |
| 의사코드 3건 | 향후: AI 생성 엔트리 코드블록 "의사 코드" 패턴 자동 경고 검토 | validate-content |

## Anti-Rationalization Guard

### 바이어스 점검
- "특이사항 없음"? → 아니다. draft 21개를 놓친 것은 검증 프로세스 부재. 위에 기록함
- 외부 귀인? → "세션이 많아서 쌓였다" → **내가 엔트리 생성 시 status 검증을 안 한 것**으로 리프레이밍
- 어려운 부분 건너뜀? → 개별 엔트리 내용 깊이 보강(본문 리라이팅)은 하지 않았다. 이번 스프린트 스코프가 "점검 + 구조 수정"이었으므로 의도적 스코프 제한이지만, 내용 품질 깊이 보강은 별도 세션 필요
- 테스트 없이 추정? → 빌드 성공으로 구조적 정합성은 확인. 런타임 렌더링은 미확인 (Vercel 배포 후 확인 필요)
