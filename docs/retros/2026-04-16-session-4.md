# 2026-04-16 세션 4 회고 — Solution → Validator 승격 사이클

## 이번에 한 것

1. **이전 세션 핸드오프 인수** — 리모트 fetch + Session 2/3 작업 (15 commits, Flow Map 시리즈 완주 + NEXT.md 87% 감축 + Search index lazy fetch + Context scaling 3-layer + dangling 4건 수정) 인수
2. **Mermaid `<br/>` warning 시스템 박제** — `mermaid-fix.mjs`에 `detectUnquotedSpecialCharLabels()` 추가, `validate-content.mjs`가 warnings를 console.warn으로 출력
3. **vitest 9 케이스 추가** — 7→16. idempotency 별도 검증 (5회 실행 결과 동일) + cylinder false negative 의도적 명시
4. **즉시 발견된 9건 수정** — five-levers 7건 + vector-search-basics 1건. solution 박제만 했던 시기에는 잠재 부채로 남아있던 것
5. **Harness Journal 024 작성** — "solution N=3+ 누적 시 validator로 승격" 패턴 자체를 박제

## 잘된 것

- **한 사이클 한 메시지 원칙 지킴** — Mermaid warning 시스템 → 9건 즉시 발견 → Journal 024로 패턴 박제까지 한 흐름. 다른 주제 섞지 않음 (memory rule "한 세션 2-3 Journal" 준수, 실제로는 1 Journal)
- **warning-only 결정의 정당화 명확** — auto-fix가 아닌 warning-only를 선택한 3가지 이유(idempotency · shape 다양성 · memory rule)를 mermaid-fix.mjs 주석 + Journal 024에 모두 명시. 미래 누군가가 "왜 auto-fix 안 했지?" 의문을 가지지 않게 박제
- **idempotency 케이스를 별도 테스트로 분리** — Bug 2 트라우마의 산물. 새 정규식 추가 시 누락하기 쉬운 검증을 의도적으로 강조
- **승격 시점에 잠재 부채 발견 패턴 입증** — 9건 즉시 발견은 "solution → validator 승격이 의미 있다"는 양적 증거. Journal 024의 핵심 운영 데이터

## 아쉬운 것

- **NEXT.md의 Mermaid 큐 항목이 "warning만이라도"로 요청됐는데 실제로는 한 발 더 나감** (즉시 9건 수정 + Journal 박제까지). 큐 정의가 작업 후 일관되지 않음. 정의 자체를 완료 시점에 갱신하는 습관 필요
- **시간 초기화 안 한 채로 작업** — 세션 시작 시 git fetch 했지만 다른 워커 프로젝트(moneyflow/tarosaju) git 상태 확인 안 함 (이번 작업이 ai-study 내부 한정이라 영향 없었지만, NEXT.md Phase 3 체크리스트 형식적으로는 미준수)

## 다음에 적용할 것

1. **N=3 룰을 compound-engineering-philosophy 엔트리에 명시 추가** (Journal 024 다음 후보로 적힘)
2. **JSX trap detector도 동일 패턴으로 확장 검토** — `{worker}` 같은 본문 중괄호. 현재 `api-contract-as-3-client-source-of-truth.mdx Line ~213`에 1건 잡혀있음
3. **Context Scaling Layer 3 POC** — 여전히 큐에 있음. Chroma 로컬 + 섀도우 모드 1주 운영 — 이건 큰 작업이라 별도 세션 권장

## Compound Assets

이번 세션이 만든 *재사용 가능한* 자산:
- **`detectUnquotedSpecialCharLabels()` 함수** — 다른 패턴(JSX trap, 다른 마크업 lang) 검출에 패턴 재사용 가능
- **vitest idempotency 케이스 템플릿** — 정규식 변경 시 5회 실행 invariant 검증 패턴
- **"Solution → Validator 승격" 메타 패턴** — Journal 024로 박제. 다음 N=3+ 솔루션 카테고리(예: react-patterns, ai-pipeline)에 적용 가능

## 수치

| 항목 | 값 |
|------|-----|
| 세션 길이 | 단일 사이클 (1 Journal) |
| 변경 파일 | 6 (logic 3 + 콘텐츠 2 + Journal 1) |
| 추가 라인 | +371 |
| 삭제 라인 | -13 |
| 신규 vitest | 9 (총 16, 100% 통과) |
| 즉시 발견된 잠재 함정 | 9 |
| 수정 완료 | 9/9 |
| 빌드 warnings (Mermaid) | 0 (수정 후) |
| 엔트리 | 105 → 106 |
