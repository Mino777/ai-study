---
date: 2026-04-13
scope: terminal 복귀 세션 (8 push 사이클, 4 에이전트 병렬 ×2, 자가손상 도구 발견)
---

# 회고: 2026-04-13 — 맥앱 부산물 정리 + 인프라 강화 + 콘텐츠 사실 정정

## 이번에 한 것

- **터미널 복귀 트리거**: 사용자 *"클로드앱으로 하다가 너무 이상해서 다시 터미널로 왔어"* — 맥앱 Claude 세션이 3 프로젝트(`ai-study` + `mino-moneyflow` + `mino-tarosaju`)에 동시에 silent 부산물을 남긴 상황
- **8 push 사이클** 진행, 7회 에이전트 병렬 활용 (2 사이클: 4 + 3 에이전트), 외부 fetch 0
- **신규 콘텐츠 4개**:
  - Harness Journal 019 — 맥앱 부산물 6 함정 + 5단 크로스 세션 리뷰 프로토콜
  - compound-engineering-philosophy.mdx (275줄) — 12 referencing entries dangling 해소 + 12 원칙 박제
  - vector-search-basics.mdx (120줄) — RAG 기초 스텁
  - tokenomics applied-log entry — 카탈로그에서 메타 분리
- **인프라 5개**:
  - `.claude/hooks/no-company-names.sh` (회사명 PreToolUse 차단)
  - `.claude/commands/cross-session-review.md` (5단 프로토콜 슬래시 커맨드)
  - `scripts/lib/mermaid-fix.mjs` (함수 추출)
  - `scripts/__tests__/validate-content.test.mjs` (6 회귀 테스트)
  - vitest.config.ts (`scripts/__tests__/` include)
- **사이드바 series-based 일반화 refactor** + 17 frontmatter 추가 (harness-journal 18개 모두 series 필드)
- **콘텐츠 사실 정정 8건**:
  - iOS Journal 000~005 (4 에이전트 병렬, 가짜 코드/카운트/스킬 전부 폐기)
  - tokenomics 카탈로그 재구조화 (503 → 443줄)
  - apple-intelligence-api §3 손상 복구
- **사설 식별자 익명화 9건** (두 차례 발견)
- **자가손상 도구 두 버그 fix** (validate-content.mjs slicing offset + regex 누적)
- **stale worktree 정리** (`sweet-napier`, branch 보존)

## 잘된 것

- **에이전트 병렬 효율**: 2회 사이클 (4 에이전트 + 3 에이전트) 모두 충돌 0건. 각 에이전트 prompt에 source 자료 inline 전달 → 외부 fetch 0회 → 성공률 100%
- **자가손상 도구 발견 → 회귀 테스트로 박제**: `validate-content.mjs`의 두 silent 손상 버그(slicing offset / regex 누적)를 발견하고, 함수 추출 + 6 vitest 케이스로 회귀 방지 시스템 완성. *자동 수정 도구는 자가 검증 없이는 신뢰 X* 라는 메타 교훈
- **메모리 위반 → 행동 레벨 차단까지 escalate**: 사설 식별자 노출 사고 발생 → 메모리 파일에만 적혀 있던 룰을 본인이 위반 → 즉시 발견 + 정정 + 메모리 강화 + **PreToolUse 훅으로 행동 레벨 차단**까지 도달. *기억 가드 → 행동 가드* 사이클 1회 완성
- **사용자 발견을 즉시 정정**: 학습 히트맵 가짜 화요일을 사용자가 스크린샷으로 발견 → 원인 추적 → iOS Journal 000~005 frontmatter 가짜 backdate 정정 → 8 entries 정직한 표시
- **Journal 019의 5단 프로토콜을 슬래시 커맨드로 즉시 박제**: 회고 → 박제 → 재사용 가능한 인프라까지 한 사이클에. 다음 세션이 `/cross-session-review`로 즉시 사용 가능
- **dangling connections 13 → 0**: 12개 referencing 엔트리가 가리키던 `compound-engineering-philosophy` 신규 작성 + vector-search-basics 신규 스텁으로 모든 dangling 해소

## 아쉬운 것

- **에이전트 prompt 익명화 지시 누락**: 4 에이전트에게 iOS Journal 재작성을 위임할 때 source 자료(사설 repo 디렉토리)를 inline으로 전달했지만 *출력에서 익명화하라*는 지시를 빠뜨림. 결과: 5건 사설 식별자 노출. 메모리에 룰이 박제되어 있었지만 본인이 prompt 작성 시 신경 못 씀. **메모리 의존의 한계 노출** — 즉시 PreToolUse 훅으로 행동 레벨 차단 추가
- **사설 식별자 노출 두 차례 발견**: 1차 정정 후 특정 패턴이 4건 더 남아있는 걸 사용자가 다시 발견. *grep을 한 번에 충분히 넓게 안 했음*. 첫 grep에서 일부 패턴만 검사하고 나머지는 빠뜨림. 처방: 메모리 파일에 금지어 *완전한 목록*을 명시 + 훅이 모든 패턴 검사
- **자동 수정 도구의 silent 손상이 누적적이었음**: validate-content.mjs의 두 버그(slicing + regex)가 *여러 세션에 걸쳐* 여러 파일을 손상시켰을 가능성. 이번에 발견한 5-quote 흔적은 빙산의 일각일 수도. 다음 세션에 grep으로 다른 파일 검증 필요
- **worktree 정찰을 처음에 안 함**: Journal 019의 stale worktree 사례를 박제하면서도 본인 프로젝트의 `sweet-napier` worktree는 마지막에야 정찰. *내가 박제한 패턴을 즉시 자기 검증에 적용 안 함* — 다음 세션 시작 시 routine으로 `git worktree list` 추가

## 다음에 적용할 것

- **에이전트 위임 prompt 템플릿에 익명화 지시 명시**: source 자료에 사설 repo 데이터가 포함되면 prompt 마지막에 *"⚠️ 출력에서 모든 사설 식별자를 익명화 (정확한 매핑은 메모리 참조)"* 한 줄 필수
- **세션 시작 routine에 `git worktree list` 추가**: NEXT.md의 *"다음 세션 시작 체크리스트"*에 worktree 정찰 한 줄 추가
- **자가손상 grep 수동 실행 (다음 세션)**: 다른 mermaid 블록에 5-quote 누적 잔재가 있는지, 다른 자동 수정 도구의 silent 손상 흔적이 있는지 전수 grep
- **`/cross-session-review` 실제 dogfooding**: 다음 맥앱/외부 세션 결과물에 적용해서 슬래시 커맨드의 실효성 검증

## Compound Assets

- **신규 슬래시 커맨드**: `/cross-session-review` — Journal 019의 5단 프로토콜 자동화. 6 함정 grep 체크리스트 박제
- **신규 PreToolUse 훅**: `no-company-names.sh` — Edit/Write 직전 회사명 차단. 화이트리스트 (메모리 / 훅 자체)
- **신규 vitest 회귀 테스트**: `scripts/lib/mermaid-fix.mjs` 추출 + 6 케이스. 두 과거 버그(slicing / regex)에 대한 docstring 경고
- **메모리 강화**: `feedback_company_project_names.md` — 위반 사례 박제 + grep 가드 프로토콜 + 에이전트 위임 익명화 룰
- **사이드바 generic series grouping**: `SERIES_LABELS` 매핑 한 줄로 새 시리즈 자동 그룹화 — frontmatter 기반 인프라
- **Compound Engineering Philosophy 박제**: 12 원칙 + 4단계 루프 + 5단계 ladder + 매핑 테이블. 12 referencing entries의 dangling 해소 동시에 *철학적 토대* 문서화
- **크로스 세션 리뷰 프로토콜**: 6 함정 (PR 메타데이터 거짓말 / 과잉설계+소비자0 / 빌드검증 누락 / Codex 오인용 / stale worktree / 자동수정 도구 손상)

## 수치

- **commits**: 8 (`d8b2dd8` → `c6aefe2`)
- **신규 파일**: 9 (콘텐츠 4 + 인프라 5)
- **수정 파일**: 27+
- **에이전트 launches**: 7 (4 + 3)
- **MDX 파일 수**: 63 → 66 (+3)
- **vitest passed**: 13 (기존 7 + 신규 6)
- **dangling connections**: 13 → 0
- **사고 재발률**: 1건 (회사명, 행동 레벨 차단까지 도달)
- **회사명 익명화 정정**: 9건 (5 + 4 두 번 발견)
- **자가손상 버그 발견 + fix**: 2 (slicing offset + regex 누적)
- **빌드 static pages**: 84

## 다음 마일스톤

다음 세션 시작 큐 (NEXT.md 박제):
- 🔴 `/cross-session-review` 첫 dogfooding
- 🟡 학습 히트맵 캘린더/년 토글
- 🟡 iOS Journal 006+ (라이브 트리거 대기)
- 🟢 LLM-as-Judge 데이터 분석
- 🟢 Tokenomics 새 레버 탐색
