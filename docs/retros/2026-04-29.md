# 스프린트 회고 — 2026-04-29 (세션 26)

iOS Harness Journal 012 엔트리화 세션.

## 한 것

- gma-ios `chore/claude-setup` 브랜치 2일치(총 18커밋) 분석
- compound 문서(retro 2건 + solutions 1건) + git log → ios-ai-journal-012 MDX 생성
- 초기 작성 후 당일 추가된 9커밋 반영 업데이트 (3-tier, wait-for-ui, screenshot-diff, zstd, onboarding)
- ai-study main push

## 잘된 것

- **compound 문서가 저널 품질을 크게 높였다.** gma-ios의 `docs/retros/2026-04-29.md` + `docs/solutions/architecture/2026-04-29-ui-test-smoke-forensic-split.md` 두 파일이 있었기 때문에 git log만 읽을 때보다 맥락, 측정값, 교훈이 훨씬 풍부하게 담겼다. compound를 잘 써두면 저널화 비용이 거의 0에 수렴한다.
- **두 단계로 나눠서 엔트리화한 것이 좋았다.** 처음 엔트리 작성 후 "더 추가된 커밋 있어"라는 요청으로 자연스럽게 업데이트로 이어졌다. 단일 엔트리에 전체 스프린트를 담을 수 있었다.
- **회사 프로젝트명 sanitize가 자연스러웠다.** 훅 가드(no-company-names.sh) + 메모리 룰이 있어서 별도 지시 없이도 `gma-ios`, `GreenCar` 없이 "사내 iOS 앱", "iOS 프로젝트"로 표현했다.

## 아쉬운 것

- **프로젝트 경로 추측 실패.** `~/gma-ios`라고 먼저 시도했다가 `no such file` 에러. `~/Develop/gma-ios`가 실제 경로. 메모리에 워커 프로젝트 경로가 없어서 find로 탐색하는 비용 발생. 자주 쓰는 레포 경로는 메모리에 박제해두면 좋다.
- **compound 이전에 엔트리화 시작.** 사용자가 compound 완료 후 핸드오프 요청을 했는데, 실제로는 gma-ios에 compound 커밋이 이미 있었음. 하지만 ai-study 쪽 compound(이 문서)는 세션 26 작업 후 처음 실행. 타이밍상 문제는 없었지만 구분이 약간 불명확했다.

## 다음에 적용

- [ ] 자주 사용하는 워커 레포 경로를 메모리에 저장 (`~/Develop/gma-ios` 등)
- [ ] journal 엔트리화 요청 시 먼저 `docs/retros/` + `docs/solutions/` 확인 → compound 문서 유무 파악 후 진행

## Compound Assets (재사용 자산)

- **패턴**: `git log + retro doc + solutions doc → 위키 엔트리` 3-source 저널화. compound를 잘 써두면 외부 레포 작업도 ai-study에 빠르게 박제 가능.
- **패턴**: 엔트리 초기 작성 → "추가 커밋 있어" 업데이트 2-pass 흐름. 한 번에 완벽하게 쓰려 하지 말고 커밋 단위로 추가하면 자연스럽다.

## 측정값

| 항목 | 수치 |
|------|------|
| 엔트리 수 | 177 → 180 |
| journal-012 줄 수 | ~500줄 (초기 378 + 업데이트 122) |
| 참조한 gma-ios 커밋 수 | 18 (9 Phase 1 + 9 당일 추가) |
