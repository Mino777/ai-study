# 세션 16d 회고 — 워커 재료 박제 + Claude Design + 엔트리 정비

**날짜**: 2026-04-19
**범위**: projects-sync → 워커 재료 수집 → 엔트리 생성/병합 → 긱뉴스 디버깅 → hub-dispatch

## 이번에 한 것

1. **projects-sync 실행** — moneyflow/tarosaju/aidy 3프로젝트 상태 진단
2. **워커 재료 박제 3건**:
   - AgentCompiler 동적 라우팅 (moneyflow #133 + aidy ADR-011 대비)
   - 티어별 AI 비용 상한 (moneyflow #134) — tokenomics→backend-ai 카테고리 이동
   - AI API 과금 방지 엔트리 confidence 업데이트 (tarosaju 실 구현 반영)
3. **긱뉴스 auto-lesson 디버깅** — cron 미실행 확인 → 수동 트리거 → `actionable-insight` 라벨 미생성이 실패 원인 → 해결
4. **aidy 엔트리 확인 + 보강** — multi-agent-sprint-optimization-patterns 빌드 에러 수정 + aidy solutions 3건 반영
5. **Claude Design 엔트리** — 웹서치 3소스 교차 검증 → 엔트리 생성 → 기존 harness-engineering 엔트리와 중복 발견 → 병합
6. **hub-dispatch Issue 2건** — aidy-architect#3, moneyflow#136
7. **ai-agent-start-here 404 링크 수정** — ../../SPEC.md, ../../CLAUDE.md 3곳

## 잘된 것

- **projects-sync로 시작** — 워커 상태 먼저 파악 후 재료 수집. 체계적 흐름
- **중복 엔트리 즉시 발견+병합** — Claude Design 엔트리를 만들었는데 이미 있는 걸 유저가 스크린샷으로 알려줌 → 즉시 병합하고 고유 내용만 추가
- **긱뉴스 디버깅** — 실패 원인을 단계별로 추적. 핵심 파이프라인(큐레이션+PR+머지)은 정상, 부수 단계(라벨)만 실패 → 라벨 생성으로 해결

## 아쉬운 것

- **Claude Design 중복 엔트리 생성** — 기존 엔트리 검색 시 `content/` 디렉토리만 grep했고 harness-engineering 카테고리는 확인했지만 파일명이 달라서 놓침. 검색 범위를 넓히거나 제목 기반 검색을 했어야 함
- **카테고리 판단 미스** — tier-based-ai-cost-cap을 처음에 tokenomics에 넣었다가 유저 피드백으로 backend-ai로 이동. 첫 판단에서 "토큰 효율성 증대"와 "서버 비용 관리"의 차이를 구분했어야

## 다음에 적용할 것

- 새 엔트리 생성 전 `npm run search -- "<주제>" 3`으로 JIT 검색 먼저 실행 → 중복 방지
- tokenomics 카테고리는 **토큰 효율성 증대** 관점의 엔트리만 (유저 피드백 반영)

## Compound Assets

| 유형 | 파일 | 비고 |
|------|------|------|
| 엔트리 | agents/agent-compiler-dynamic-routing.mdx | 신규 |
| 엔트리 | backend-ai/tier-based-ai-cost-cap.mdx | 신규 (tokenomics→backend-ai 이동) |
| 엔트리 | agents/multi-agent-sprint-optimization-patterns.mdx | 보강 |
| 엔트리 | harness-engineering/claude-design-visual-prototyping-workflow.mdx | 병합 |
| 엔트리 수정 | backend-ai/ai-api-cost-prevention-serverless-security-pattern.mdx | conf 1→2 |
| 엔트리 수정 | harness-engineering/ai-agent-start-here.mdx | 404 링크 수정 |
| 엔트리 삭제 | frontend-ai/claude-design-ai-native-design-tool.mdx | 중복 제거 |
| GitHub | aidy-architect#3, moneyflow#136 | hub-dispatch |
| GitHub | actionable-insight 라벨 생성 | 긱뉴스 파이프라인 수정 |

## 수치

| 항목 | Before | After |
|------|--------|-------|
| 엔트리 수 | 137 | 142 |
| 커밋 수 (이 세션) | — | 8건 |
| 빌드 에러 | 2건 | 0건 |
| 404 링크 | 3건 | 0건 |

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| Claude Design 중복 생성 | 엔트리 생성 전 JIT 검색 필수 | (피드백 메모리) |
| tokenomics 카테고리 혼동 | tokenomics = 토큰 효율성 증대만 | (피드백 메모리) |

## For AI Agents

- 이 세션은 **워커 프로젝트 재료를 허브로 박제하는 수집 세션**
- projects-sync → 재료 특정 → 엔트리 생성 → 중복 발견 시 병합 흐름
- 중복 방지: 엔트리 생성 전 `npm run search` 또는 `Glob` 으로 기존 파일 확인 필수
