# 2026-04-24 회고 — Skillify 후반: Step 5/7 도구 + 52→100% + CI

- **스프린트**: Session 21 후반 (04-23 늦은 저녁 ~ 04-24 이른 오후)
- **시작 상태**: main clean, Step 8 완료, Step 5/7 미착수
- **끝 상태**: Step 5/7 도구 + golden set 48 케이스 + 100% accuracy + weekly CI + 사설 식별자 전역 0건

## KPI 판정

| 지표 | baseline | target | actual | 달성률 | verdict |
|------|----------|--------|--------|--------|---------|
| resolver-eval accuracy | 52% | 80% | 100% | 171% | exceeded |
| resolver-eval cases | 25 | 40 | 48 | 153% | exceeded |
| 엔트리 수 | 163 | 170 | 165 (+1 journal +1 auto-lesson) | 29% | stagnant (의도적 deprioritize) |
| ccusage 베이스라인 | 미구축 | 구축 | 미구축 | 0% | failed (tool 미설치) |

**종합 verdict**: 핵심 축(Skillify)에서 exceeded → **action: scale_up** (다른 repo로 이식). 콘텐츠는 의식적 deprioritize — 다음 스프린트 재배치.

## 이번에 한 것

| 영역 | 변경 | PR |
|---|---|---|
| Step 5 | `scripts/extract-failure-moments.mjs` + vitest 6 | #80 |
| Step 7 | `scripts/resolver-eval.mjs` + vitest 5 + 25 케이스 | #80 |
| Accuracy | tokenizer + Korean 키워드 → 52→100% | #81 |
| CI | weekly-resolver-eval.yml (threshold 80%, Issue auto) | #82 |
| Scale | Golden set 25→48, 100% 유지 | #83 |
| Hook | stdin 누락 버그 + fail-closed 전환 | 7ba29fe |
| 익명화 | 사설 식별자 역사적 29건 정리 | 6d7e583 |

## 잘된 것

- **실습 과제를 같은 세션에 실행**: /ingest에서 "/scripts/check-skills-reachable.mjs MVP를 오늘 중 작성"이라 걸어둔 실습 과제를 Phase B에서 바로 구현 → dogfood → gap 발견 → 수정 loop 완주. feedback_dogfood_in_same_session.md 실천 예시.
- **dogfood가 훅 버그 노출**: Journal 027 Write에서 `gma-ios` 포함 콘텐츠가 막혀야 했는데 통과된 게 유저가 알아챌 때까지 몰랐음. 하지만 stdin 버그를 스스로 찾아 fail-closed로 교정까지 완료 (재발 방지 포함).
- **세션 안에서 52→100% 완주**: tokenizer 개선 + routing 한국어 키워드 + regex 버그 (review rule 괄호 꼬리)까지 3단 개선을 한 흐름에서 끝냄. 중간에 멈추고 다음 세션 넘겼으면 파편화됐을 것.
- **merge-back 메모리 자동 활용**: 4 PR 모두 worktree → PR → auto-merge → 즉시 정리 + main ff-pull 패턴으로 진행. 2026-04-23 박제한 `feedback_worktree_merge_back.md`가 반사행동화됨.

## 아쉬운 것 / 판단 실수 (Anti-Rationalization)

리프레이밍한 실수 4건:

1. **훅이 작동 안 하는 걸 실시간에 못 잡음** — Journal 027 Write 시점에 `gma-ios` 포함 콘텐츠가 통과될 때 "아, 훅이 왜 안 막지?" 의심했어야 함. 유저 제보로 뒤늦게 발견. **리프레이밍**: "훅 로직은 OK겠지" 근거 없는 가정.
2. **resolver-eval 첫 실행 시 rules 20개 보고도 의심 안 함** — review rule의 `(2-Stage...)` 꼬리 때문에 regex `$`에 안 걸려 파싱에서 빠져 있었음. rules 20 vs 실제 라우팅 테이블 21줄 mismatch를 한 줄 세는 것만으로 감지 가능했음. **리프레이밍**: "대충 파싱 잘 되겠지" 관성.
3. **null 케이스 threshold 0.3을 임의로 골라두고 검증 안 함** — "Skillify Step 8 감사 돌려줘"가 design-review에 0.333으로 매치돼서 fail — 0.3 threshold가 너무 낮다는 걸 golden set 쌓은 뒤에야 깨달음. **리프레이밍**: "기본값 잡고 가자"가 "검증 미룬" 것과 같음.
4. **ccusage 미설치 상태를 KPI 세울 때 확인 안 함** — 지난 스프린트에서도 "토큰 절감 미측정" 방치 패턴 똑같이 반복. 이번에도 KPI 넣어놓고 실제 tool 있는지는 뒤늦게 체크. **리프레이밍**: "시간 없어서"가 아니라 "사전 확인 우선순위 판단 실패".

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|------|------|------|
| 훅 silent pass (envvar 스키마 drift) | fail-closed 기본값 + stdin 우선 + 간접 지칭 패턴 확장 | .claude/hooks/no-company-names.sh |
| 사설 식별자 29건 잔존 | Public wiki + docs 전수 익명화 배치 | content/, docs/ 10 파일 |
| 한국어 intent ↔ 영문 routing mismatch | CLAUDE.md routing 10 엔트리 한국어 키워드 보강 | CLAUDE.md |
| resolver-eval 회귀 가능성 | weekly CI (매주 + CLAUDE.md 변경 시 + Issue 자동 생성/close) | .github/workflows/weekly-resolver-eval.yml |
| golden set 25 케이스 단일 표현 편향 | extract-failures 씨드 + 다양한 표현으로 48 확장 | data/resolver-eval-cases.json |

## Compound Assets

- `scripts/extract-failure-moments.mjs` — 세션 transcript → eval 씨드 (한/영 14 패턴)
- `scripts/resolver-eval.mjs` — structural routing eval (LLM 없이 결정론적)
- `data/resolver-eval-cases.json` — 48 golden cases (null 4개 over-routing 체크 포함)
- `.github/workflows/weekly-resolver-eval.yml` — 회귀 감시 + Issue 자동화
- `docs/solutions/workflow/2026-04-24-fail-closed-hook-defense.md` — 훅 퇴화 방어 원칙
- CLAUDE.md `## Skill routing` — 21 규칙, 한국어 intent 100% 커버

## 다음에 적용할 것

1. **다른 workflow에 fail-closed 감사** — `.github/workflows/*`와 `.claude/hooks/*` 전수 점검. "입력 비어도 exit 0" 패턴 찾아 fail-closed로 전환.
2. **"도구 실제 작동 확인" 반사행동** — 정책 훅 자체 테스트 체크리스트를 `.claude/commands/validate-hooks.md` 신규 스킬로 박제 검토.
3. **KPI 사전 검증** — 다음 KPI 세팅 시 측정 도구 존재 여부부터 확인 (`which ccusage` 먼저).
4. **워커 6개에 resolver-eval 이식** — 허브의 accuracy만 관리하는 건 반쪽. 각 워커 CLAUDE.md routing도 같은 감시 적용.

## 2차 방어선

이 세션 주요 커밋 **4건 (#80 #81 #82 #83)** + 서브 2건 (훅 fix, 역사적 익명화). `/cross-session-review` 유용할 영역:
- resolver-eval.mjs의 tokenizer 특이 케이스 (특수문자, 영문/한글 혼용)
- 훅 fail-closed 전환이 다른 정상 플로우를 깨지 않는지 (e.g., MCP tool 호출)
- 48 케이스가 실제 LLM 라우팅과 얼마나 상관이 있는가

## 수치

- 세션 길이: 2026-04-23 저녁 ~ 2026-04-24 오후 (compact 0회, context 깊음)
- PR 머지: 4 (#80/81/82/83) + 3 interim fix (e972a35 compound, 7ba29fe hook, 6d7e583 anonymize, 16eff6d path, e2b70d8 journal)
- 신규 스크립트: 2 (extract-failure-moments, resolver-eval)
- 신규 vitest 스위트: 2 (+11 케이스, 총 58)
- 신규 솔루션: 1 (fail-closed-hook-defense)
- 신규 엔트리: 1 (Journal 027)
- Accuracy 이동: 52% → 68% → 92% → 96% → 100% (25 → 48 케이스)
- Golden set 크기: 25 → 48 (+92%)
- 익명화: 29 → 0 (역사적 잔존)
