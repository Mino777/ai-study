# 2026-04-18 세션 13 회고 — CI 안정화 + 큐 정리

## 이번에 한 것

1. **npm audit CI 실패 해결** — dompurify 패치 + `--omit=dev`로 devDep 제외
2. **미완성 마커 빌드 경고 2건 해소** — false positive 표현 수정
3. **validate-mdx Trap 3 stress test** — 코드블록 밀집 파일 5개 대상 FP율 측정

## 잘된 것

- 큐 3건을 30분 내에 정리. audit 문제의 근본 원인(devDep 체인)을 정확히 파악해서 프로덕션 영향 없이 해결
- stress test에서 기존 설계 의도("수동 검토 플래그")가 맞다는 것을 데이터로 확인 — 불필요한 수정 안 함

## 아쉬운 것

- validate-mdx stress test 결과가 "현상 유지"로 끝남. FP율 90%+를 확인했으면 코드블록 경계 인식 개선 방안(AST 기반 파싱 등)까지 프로토타입해볼 수 있었는데, "의도대로 동작"이라는 판단에서 멈춤. **스코프를 줄인 것이 맞는 판단이었는지, 아니면 어려운 부분을 건너뛴 것인지** 다음 세션에서 재평가 필요

## 다음에 적용할 것

- devDependency 취약점이 CI를 차단하는 패턴은 다른 프로젝트에서도 발생 가능 — moneyflow/tarosaju에 audit 게이트 추가 시 처음부터 `--omit=dev` 포함
- validate-mdx Trap 3 개선은 FP가 실제 사고를 유발할 때(누군가 FP 때문에 진짜 문제를 무시하는 상황) 착수

## Compound Assets

| 유형 | 파일 | 내용 |
|---|---|---|
| CHANGELOG | CHANGELOG.md | 세션 13 섹션 추가 |
| 회고 | docs/retros/2026-04-18.md | 이 파일 |

## 수치

| 항목 | 값 |
|---|---|
| 커밋 | 2건 |
| CI 상태 | audit 통과, 빌드 경고 0건 |
| 테스트 | 23/23 통과 |
| 엔트리 수 | 129 (변동 없음) |
| JIT totalQueries | ai-study:6, moneyflow:4, tarosaju:3, architect:11 |

## Anti-Rationalization Guard

### 바이어스 점검
- "특이사항 없음" 아님 — stress test 스코프 판단에 대한 자기 의심 기록함
- 외부 귀인 없음 — 모든 판단이 내 선택
- 테스트 없이 추정한 코드 없음 — audit fix와 빌드 모두 로컬 검증 완료
- 스코프 축소: validate-mdx AST 파싱은 의식적으로 보류 — 현재 FP가 사고를 유발하지 않으므로 적절한 판단이라 봄

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|---|---|---|
| devDep 취약점이 CI 차단 | `--omit=dev` 패턴 적용 | ci.yml |
| 미완성 마커 FP | 완료 상태 반영 표현 수정 | Journal 003, Tokenomics catalog |
