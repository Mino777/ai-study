# 2026-04-18 세션 13 회고 — CI 안정화 + 큐 정리 + 워커 재료 박제

## 이번에 한 것

1. **npm audit CI 실패 해결** — dompurify 패치 + `--omit=dev`로 devDep 제외
2. **미완성 마커 빌드 경고 2건 해소** — false positive 표현 수정
3. **validate-mdx Trap 3 stress test** — 코드블록 밀집 파일 5개 대상 FP율 측정
4. **3프로젝트 projects-sync** — moneyflow/tarosaju/aidy-architect 상태 스냅샷
5. **워커 재료 체크 + 엔트리 박제** — 3프로젝트 병렬 탐색 → 신규 2건 + 보강 1건
6. **Zod 5-Layer 엔트리 갱신** — "미적용" → 양쪽 실전 적용 데이터 추가

## 잘된 것

- 큐 3건을 30분 내에 정리 후, 워커 프로젝트 재료까지 체크하여 세션 활용도 극대화
- 3프로젝트 병렬 Explore agent로 재료 체크 → 중복 2건(Stall Detection, GITHUB_TOKEN) 사전 감지하여 불필요한 엔트리 생성 방지
- Zod 5-Layer 엔트리가 "제안" 단계에서 "실전 검증" 단계로 격상 — 위키 신뢰도 상승

## 아쉬운 것

- projects-sync에서 aidy-architect를 빠뜨려서 유저가 지적함. **scope 결정을 NEXT.md/CLAUDE.md 기반으로 했는데, projects-sync 커맨드 스펙에 moneyflow+tarosaju만 명시되어 있어서 aidy를 누락**. 커맨드 스펙을 맹신한 판단 실수 — 유저의 실제 관리 범위를 먼저 확인했어야 함
- validate-mdx stress test "현상 유지" 판단이 AST 파싱 개선 기회를 놓친 것인지 여전히 불명확

## 다음에 적용할 것

- `/projects-sync` 실행 시 aidy-architect 포함 (메모리에 저장 완료)
- devDependency 취약점 CI 차단 패턴 — 다른 프로젝트에 audit 게이트 추가 시 처음부터 `--omit=dev`
- 워커 재료 체크는 기존 엔트리 중복 확인을 **먼저** 하고 신규 작성 (이번에는 잘 했음, 패턴으로 고정)

## Compound Assets

| 유형 | 파일 | 내용 |
|---|---|---|
| CHANGELOG | CHANGELOG.md | 세션 13 전체 섹션 |
| 회고 | docs/retros/2026-04-18.md | 이 파일 |
| 솔루션 | docs/solutions/github-actions/2026-04-18-npm-audit-devdep-ci-failure.md | devDep audit 패턴 |
| 신규 엔트리 | content/prompt-engineering/empathetic-ai-prompt-techniques.mdx | Rogerian/MI/NVC |
| 신규 엔트리 | content/infrastructure/parallel-worktree-git-lock-trap.mdx | Git lock race condition |
| 보강 | content/harness-engineering/ai-output-zod-validation-pattern.mdx | 실전 적용 갱신 로그 |
| 메모리 | feedback_projects_sync_includes_aidy.md | 3프로젝트 진단 룰 |

## 수치

| 항목 | 값 |
|---|---|
| 커밋 | 5건 |
| CI 상태 | audit 통과, 빌드 경고 0건 |
| 테스트 | 23/23 통과 |
| 엔트리 수 | 131 (+2) |
| JIT totalQueries | ai-study:6, moneyflow:4, tarosaju:3, architect:11 |

## Anti-Rationalization Guard

### 바이어스 점검
- "특이사항 없음" 아님 — aidy 누락 판단 실수 기록함
- 외부 귀인 체크: "커맨드 스펙에 없어서"는 외부 귀인 → 리프레이밍: **유저의 실제 관리 범위를 확인 안 한 내 판단 실수**
- 테스트 없이 추정한 코드 없음 — 모든 엔트리 빌드 검증 완료
- 스코프: 워커 재료 중 2건은 기존 엔트리 중복으로 의식적 스킵 — 적절한 판단

### 2차 방어선
주요 커밋 5건 → `/cross-session-review` 권장. 특히 신규 엔트리 2건의 내용 정확성 독립 검증 가치 있음.

## 프로세스 개선 (이번 스프린트)

| 재료 | 개선 | 파일 |
|---|---|---|
| devDep 취약점이 CI 차단 | `--omit=dev` 패턴 적용 | ci.yml |
| 미완성 마커 FP | 완료 상태 반영 표현 수정 | Journal 003, Tokenomics |
| projects-sync aidy 누락 | 메모리에 3프로젝트 룰 저장 | feedback_projects_sync_includes_aidy.md |
| 워커 재료 중복 체크 미비 | 기존 엔트리 grep 먼저 실행 패턴 확립 | (이번 세션에서 실행, 다음부터 습관) |
